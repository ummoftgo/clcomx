# Risks and Open Questions

> 이 문서는 CLCOMX "Direct Agent Runtime"의 **위험 레지스트리·확정 기본값(Resolved defaults)·Open questions 집계**의 단일 출처다. 다른 문서에서 "unverified" 또는 "결정 필요"로 올라온 항목은 모두 여기 §3 레지스트리로 모은다.
>
> **역할 분리**: 타입 정의는 [`15-data-contracts.md`](15-data-contracts.md), 규칙·불변식은 [`04-normalized-agent-model.md`](04-normalized-agent-model.md)가 권위다. 이 문서는 그 두 문서가 의존하는 **결정/가정/위험만** 다루고 타입·규칙을 재정의하지 않는다. 위험 완화 방법이 특정 설계를 인용할 때는 해당 문서 §번호로 링크한다.
>
> **읽는 법**: §1은 카테고리별 위험(영향·완화·검출). §2는 Resolved defaults(현 시점 확정 기본값과 근거). §3은 Open questions 레지스트리(질문/출처/현재 기본값/구현 전 확인 방법). 구현 에이전트는 §2를 "그대로 따르고", §3은 "구현 착수 전 확인 게이트"로 쓴다.

조사 시점: 2026-06-25. 코드 스냅샷 기준: commit `e7a5f9e`; 구현 전 현재 작업트리와 대조.

---

## 1. 위험 레지스트리 (카테고리별)

각 항목은 **영향(Impact)** / **완화(Mitigation)** / **검출(Detection)** 3축으로 기술한다. 심각도(S1=치명, S2=중대, S3=보통)는 v1 범위 기준 상대 평가다.

### 1.1 Protocol drift (S1)

Codex app-server protocol과 ACP/Claude adapter는 빠르게 변한다. 메서드명·필드·discriminant·enum 멤버가 minor 버전에서 바뀔 수 있다.

- **영향**: pinned ref(`rust-v0.142.0`, ACP SDK package `@agentclientprotocol/sdk@0.29.0`, `@agentclientprotocol/claude-agent-acp@0.51.0`)와 실제 런타임 바이너리/schema artifact가 어긋나면 adapter의 wire→`AgentEvent` 매핑(15 §3, ref-codex §8, ref-acp §13)이 조용히 깨진다. 새 notification이 무시되거나, 사라진 필드가 `undefined`로 흘러 transcript가 비거나, discriminant 변경으로 event가 unknown으로 떨어진다.
- **완화**:
  1. generated type과 runtime package 버전을 **함께 핀**한다(§2 Resolved defaults). Codex는 ref `rust-v0.142.0` 기준 mapping + 구현 산출물 `codex-cli 0.142.2` generate-ts, ACP는 wire `protocolVersion=1` + `@agentclientprotocol/sdk@0.29.0` package schema/dist 부분 미러, Claude adapter는 npm `0.51.0` 고정이다. `schema-v1.16.0`은 baseline 조사 후보로만 남긴다(OQ-41).
  2. adapter는 **unknown variant를 버리지 않고** `ProviderRef.raw`에 보존(15 §0.2)하고, unknown method/notification은 `error{recoverable:true}` 또는 debug 로그로 가시화한다(절대 silent drop 금지).
  3. `AgentRuntimeMetadata.protocolVersion`/`adapterVersion`/`providerVersion`(15 §7.1)을 세션 시작 시 기록해 호환성 추적을 남긴다.
  4. 버전 업데이트 시 schema diff 검토를 PR 체크리스트화하고, fixture replay 회귀(11 §fixture replay)를 통과해야 머지한다.
- **검출**: (a) fixture replay 테스트에서 unknown variant 카운트를 assert. (b) runtime에서 unknown method 수신 시 stderr/telemetry 카운터 증가. (c) `initialize` 응답의 `protocolVersion`이 기대값과 다르면 startup 경고.

### 1.2 Codex experimental surface (S2)

Codex app-server의 일부 기능·메서드는 `--experimental` 플래그 또는 unstable schema 뒤에 있을 수 있다(ref-codex §1.4 experimental 필드, §10 unverified).

- **영향**: 사용자에게 노출한 기능이 다음 Codex 버전에서 사라지거나 의미가 바뀐다. experimental 필드를 정식 매핑에 끌어오면 protocol drift(1.1)와 결합해 회귀가 커진다.
- **완화**:
  1. direct runtime 전체를 **실험 flag 뒤**에 둔다(§2). experimental Codex 기능은 그 안에서 다시 한 단계 gate.
  2. `#[experimental]` 필드는 normalized 타입으로 승격하지 않고 `raw`에만 보존(15 §0.2).
  3. experimental 경로 실패 시 stdio 기본 흐름으로 graceful fallback 하고, 사용자에게 experimental 경고 copy를 표시한다.
- **검출**: experimental 기능 사용 여부를 세션 metadata에 플래그로 남기고, startup 시 `initialize` capability에서 해당 기능 미지원이면 비활성화.

### 1.3 WebSocket transport 후속화 (S3)

Codex app-server는 websocket transport 가능성이 있으나 본 조사에서 wire로 검증되지 않았다(05 §"websocket listen은 후속 검증 대상", 07 §"websocket: 검증 후 optional", 15 §8.1 `AgentRuntimeStartParams` websocket variant 주석).

- **영향**: websocket을 1차에 구현하면 미검증 transport에 framing/auth/backpressure 위험이 추가된다. 실제 websocket auth token 저장 위치는 후속 wire 검증 전까지 열어 두되, v1의 비노출 하한은 09 §5의 redaction/scrub 정책으로 확정한다.
- **완화**:
  1. v1은 **stdio 전용**으로 구현한다(§2). `AgentRuntimeStartParams`에 websocket variant 타입은 future-sketch로 유지하되 Rust handler는 1차에서 start params를 **로깅/스냅샷에 노출하기 전 즉시** `Err("only jsonrpc-stdio transport is supported in v1")`로 reject한다(07 handler). variant·`authToken?`가 15 §8.1 public 계약에 존재하므로, reject가 로깅보다 늦으면 `authToken`이 관측면에 새는 누출 표면이 생긴다 — 그래서 reject가 로깅·스냅샷보다 **먼저** 일어나야 한다.
  2. `authToken`은 v1부터 09 secret scrub/redaction 집합에 포함한다(token 필드가 계약에 존재하나 v1 미사용·누출 차단). 실제 websocket auth 저장/전송 정책은 websocket wire 검증을 시작하는 후속 scope에서 다시 연다.
- **검출**: `transportKind:"websocket"`로 들어오는 호출은 handler에서 **로깅 전** 명시적 거부 + (token 미포함) 거부 로그. UI는 stdio만 노출. websocket start가 token 로깅 없이 reject되는지 `agent_runtime::tests` RS-12c/RS-18과 11 §8.5로 회귀 고정한다.

### 1.4 WSL / Windows path 처리 (S2)

ACP는 absolute path를 요구하고, CLCOMX는 WSL path·Windows path·file URI를 섞어 다룬다(15 §6 `StartSessionParams.workDir` 주석, 07 §WSL 경계).

- **영향**: 잘못된 path 종류가 adapter에 들어가면 ACP가 reject하거나 엉뚱한 cwd로 세션이 뜬다. file URI vs 경로 혼동으로 `resource`/`diff` content(15 §4)의 path 표시가 깨진다. WSL↔Windows 경로 변환 누락 시 파일 열기·diff가 동작하지 않는다.
- **완화**:
  1. adapter 입력 직전에 `workDir`를 **WSL absolute path로 canonicalize**한다(15 §6 주석). canonicalize 단일 진입점을 두고 모든 provider가 거치게 한다.
  2. path 종류(WSL path / Windows path / file URI)를 명시적으로 구분하는 유틸을 두고, content path 렌더링·파일 열기 양쪽이 같은 유틸을 쓴다.
  3. 변환 규칙·예시는 [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §WSL 경계에 정본화하고 본 위험은 그것을 인용한다.
- **검출**: relative path가 adapter 경계에 도달하면 즉시 reject(assert). path 변환 단위 테스트(WSL/Windows/URI 케이스)를 11 §에 포함.

### 1.5 Approval deadlock / pending cleanup (S1)

provider가 permission response를 기다리는 동안 UI가 닫히거나 turn이 cancel되면 agent가 멈춘다. pending request cleanup은 runtime 필수 기능이다(04 §4.2 불변식).

- **영향**: pending approval을 닫지 않으면 agent가 무한 대기하고 세션이 `requires_action`에 고착된다. 두 protocol 모두 cancel 시 pending permission을 닫는 것을 MUST로 규정한다(ACP §3.8, Codex §4.4 — 04 §4.2 인용).
- **완화**:
  1. turn cancel/shutdown(process 살아 있음) 시 unresolved approval을 `cancelled`로 닫고 wire 응답도 보낸다(04 §4.2 규칙 1–4). `process_exited`(process 사망)는 모든 pending approval을 `failed`(wire 미전송, 내부 전용)로, pending RPC를 failed로 닫는다(04 §5.0, 15 §3 `process_exited`).
  2. **deadlock 탐지 권위는 frontend pending table**이다(04 §5 authoritative cleanup). backend `AgentRuntimeSnapshot.pendingRequestIds`(15 §8.1)는 **v1에서 선택적**이며 — backend는 단순 id bookkeeping만 하거나 생략할 수 있어 **비어 있을 수 있다**(07 §6.4: 1차에서 backend pending 추적 생략 가능, 항상 빈 배열 무방). 따라서 cleanup/deadlock 판정은 frontend pending table을 권위로 하고, snapshot은 진단 보조 신호로만 쓴다.
  3. `ApprovalDecision.outcome:"failed"`는 client 내부용이며 wire로 보내지 않는다(04 §4.2 규칙 4).
  4. **미지원 server request 무응답 금지(R5, RD-15·OQ-35)**: provider server→client REQUEST(`id` 있음) 중 미지원 method도 반드시 JSON-RPC error(`-32601` 등) 또는 명시적 decline/cancel 응답을 보낸다 — silent-drop하면 provider가 영구 대기(deadlock)한다. unknown NOTIFICATION(`id` 없음)은 raw 보존+counter만(응답 불필요). adapter default 분기는 `return []`로 끝내지 않는다(05 §7.1, 06).
- **검출**: cleanup 누락 회귀 테스트(11 §): cancel/탭 닫기/process kill 각각에서 **frontend pending table**의 pending이 0으로 떨어지는지 assert(권위). backend snapshot의 `pendingRequestIds` 길이는 backend가 추적할 때만 보조 텔레메트리로 쓴다(v1 선택적, 비어 있을 수 있음 — §1.5 완화 2).

### 1.6 Terminal regression (S2)

embedded terminal과 legacy PTY가 공존하면 focus·shortcut·resize·bottom-follow 회귀가 생긴다(기존 02 위험 보존).

- **영향**: direct runtime 도입이 기존 PTY/xterm 사용자 경험을 깨뜨린다. focus 충돌, 단축키 가로채기, resize 미반영, 스크롤 follow 깨짐.
- **완화**:
  1. legacy PTY 경로는 삭제하지 않고 보조 셸/명령 출력 embed/fallback으로 유지(00 범위, 04 §3.5 legacy 처리).
  2. legacy PTY output은 transcript로 끌어올리지 않고 `terminal_output_delta`(15 §3)로만 처리한다(04 §3.5).
  3. UI 작업 전후로 기존 terminal E2E 회귀 스위트를 유지하고, PR 게이트로 둔다(11 §).
- **검출**: 기존 terminal E2E(focus/resize/follow) green 유지가 머지 조건. 신규 UI는 별도 surface로 격리.

### 1.7 Branding / product confusion (S2)

Claude Agent SDK / ACP integration이 Claude Code나 Anthropic 공식 제품처럼 보이면 안 된다(기존 위험 보존, ref-claude §1 branding note).

- **영향**: provider 표시·UI copy가 공식 제품으로 오인되면 라이선스/상표 리스크. 공식 Agent SDK overview는 third-party product가 Claude app credentials/rate limits를 제공하는 것을 허용하지 않고 API key 사용을 권장한다.
- **완화**:
  1. provider 표시는 중립적 라벨로 하고, "powered by"류 공식 제품 사칭 copy를 금지한다.
  2. 인증은 사용자 본인 자격/키 사용을 전제로 하고, claude.ai 세션 재판매·우회를 하지 않는다.
  3. branding/auth 허용 범위는 공식 Agent SDK overview 문구를 따른다(2026-06-28 확인, 레지스트리 OQ-09).
- **검출**: UI copy 리뷰 체크리스트. 외부 노출 문자열 audit.

### 1.8 Backpressure / 대용량 출력 (S2)

명령 실행이 대량 stdout/stderr를 쏟거나 streaming delta가 폭주하면 transport queue가 포화한다(15 §8.3 `agent-runtime-backpressure`, 05 §66 queue/backpressure).

- **영향**: late-attach용 replay log를 unbounded로 두면 메모리 폭증. bounded로 두면 trim된 구간이 late-attach 재구성에서 빠진다(실시간 emit은 그대로 흐름). UI 렌더가 delta 폭주로 멈출 수 있다. **단 v1의 replay log/seq는 diagnostic-only bounded log이며**(07 §7.2 정본), late-attach replay consumer(delta-since 재구성)는 후속이라 v1에는 **사용자-visible 복구 보장이 없다** — trim된 구간이 사라져도 v1 기능 손실로 보지 않는다.
- **완화**:
  1. v1 완화책은 **"bounded replay log + telemetry"**이며 **emit throttle이 아니다**(07 §7.2 정본 명명). backend는 late-attach용 replay log(`message_log`)만 bounded로 경계 짓되, 이 log/seq는 **diagnostic-only bounded log**이다 — late-attach replay consumer(delta-since 재구성)는 **후속**이라 v1에는 **사용자-visible 복구 보장이 없다**(07 §7.2). 그 경계에서 trim된 누적 건수를 `agent-runtime-backpressure{droppedMessages}`(15 §8.3) **telemetry 신호**로 알린다. **emit 자체는 막거나(throttle)·합치거나(coalesce)·떨어뜨리지(drop) 않는다** — frontend는 실시간 stream을 그대로 받는다. UI는 recoverable warning을 표시하고 pending approval은 자동 방치하지 않는다(05 §66). 실제 emit cap/coalesce/buffer drop은 **후속**이다(OQ-50).
  2. command output은 transcript content가 아니라 `command_output_delta`(15 §3)로 흐르게 하고, 대용량은 ring buffer/요약으로 캡한다.
  3. late-attach 신뢰성을 위해 message에 단조 증가 seq + snapshot/delta-since를 PTY와 동일 원리로 적용(후속, 15 §8.3 · 04 §3.4 · `research/codebase-backend.md` §2.3·§10 권고 3).
- **검출**: replay-log trim(backpressure) telemetry 카운터. 대용량 출력 fixture(수 MB stdout) replay 시 실시간 emit 누락 0 또는 replay-log trim에 대한 명시적 backpressure 신호 assert(11 §).

### 1.9 Node bin 실행 전제 (S2)

Claude ACP adapter는 node로 `dist/index.js`를 실행하고, claude native 바이너리는 SDK optional dependency 번들을 resolve한다(ref-claude §"claude native binary 해석", §"확인된 사실").

- **영향**: (a) 배포 대상에 node 런타임이 없거나 버전이 안 맞으면 adapter가 안 뜬다. (b) `npm install --omit=optional`로 설치하면 native 바이너리가 빠져 *"Claude native binary not found"* 실패(ref-claude §117). (c) `CLAUDE_CODE_EXECUTABLE`/`CLAUDE_CONFIG_DIR` 미설정 시 잘못된 자격/경로 사용(ref-claude §환경변수).
- **완화**:
  1. node 런타임 존재·버전과 Claude native binary resolve 가능 여부를 startup preflight로 확인하고, 실패 시 명확한 에러 메시지(설치 안내)로 fallback한다. Claude native probe는 `node <adapterEntryPath> --cli --version` 성격으로 실행해 SDK optional dependency 또는 `CLAUDE_CODE_EXECUTABLE` override를 세션 시작 전에 검증한다.
  2. Claude adapter 설치 시 **optional dependency 포함**을 문서화·검증한다(`--omit=optional` 금지).
  3. native 바이너리 경로는 필요 시 `CLAUDE_CODE_EXECUTABLE`로 명시 지정, config dir은 `CLAUDE_CONFIG_DIR`로 격리.
  4. 실제 배포 대상의 node/claude/SDK 번들 버전은 조사 머신 값(`node v24.14.0`, `claude 2.1.187`)과 다를 수 있으므로 target에서 재확인(레지스트리 OQ-10).
- **검출**: startup preflight(node present, non-empty version, native binary resolvable)을 세션 시작 전에 실행하고 결과를 `error{recoverable}`로 보고. target별 실제 버전·호환성 판단은 배포 검증에서 별도 확인한다.

### 1.10 Version 호환 (S2)

claude-agent-acp npm 버전, ACP schema 버전, Codex ref 버전이 서로 호환되어야 한다. 이전 조사값에는 0.50.0/0.51.0 혼선이 있었지만(OQ-08), v1 구현 기준은 exact 0.51.0 + SDK 0.29.0 package schema/dist로 해소됐다.

- **영향**: adapter 의존성 버전과 협상되는 ACP `protocolVersion`/capability가 어긋나면 세션 생성·permission·tool 매핑이 부분적으로 실패한다. 버전 혼선(0.50.0 vs 0.51.0)으로 capability 차이를 놓친다.
- **완화**:
  1. Claude adapter는 npm `@agentclientprotocol/claude-agent-acp@0.51.0`을 고정한다(§2 갱신). ACP wire는 `protocolVersion=1` 기준이며, 구현 기준 artifact는 해당 package가 고정하는 `@agentclientprotocol/sdk@0.29.0`의 package schema/dist와 CLCOMX 부분 wire mirror다(OQ-41). `schema-v1.16.0`은 baseline 후보일 뿐 구현 핀이 아니다.
  2. 0.50.0↔0.51.0 capability 차이는 더 이상 v1 gate가 아니다. v1은 exact `@agentclientprotocol/claude-agent-acp@0.51.0` + transitive `@agentclientprotocol/sdk@0.29.0` package schema/dist를 구현 기준으로 고정하고, 0.50.0 조사값은 폐기한다(OQ-08 해소). 의존성 refresh 때만 OQ-41 절차로 schema diff + fixture replay를 다시 수행한다.
  3. `initialize`/`session/new` 응답의 `protocolVersion`·capability를 metadata에 기록하고, 기대와 다르면 startup 경고(1.1과 동일 메커니즘).
  4. **런타임 중 provider 교체 경계(R-NEW-02)**: 런타임 중(앱 구동 중) 사용자가 WSL 측 codex/claude/adapter를 교체하면 기존 세션과 신규 세션의 provider 버전이 갈릴 수 있다. 세션 시작 시 `--version`을 `AgentRuntimeMetadata`(15 §7.1)에 고정하고, 동일 윈도우 신규 세션이 다른 버전을 resolve하면 경고한다. (preflight cache 무효화는 OQ-36과 연동)
- **검출**: 의존성 lock 검증 + startup capability assert. 버전 bump PR에서 fixture replay 회귀 통과 필수.

### 1.11 Multi-window registry 동시성 (S2)

[12](12-implementation-workstreams.md) T1.4의 module-level registry(handle→`runtimeId`/port 매핑 + 전역 pending request table)는 단일 윈도우 전제로 기술됐으나, CLCOMX는 멀티 윈도우 앱이라 윈도우 간 registry 수명·소유권이 경합할 수 있다(R-NEW-01).

- **영향**: 한 윈도우가 닫히며 registry 엔트리를 정리할 때 다른 윈도우의 살아 있는 `runtimeId`/pending request를 함께 날리거나, cross-window dispatch로 event가 엉뚱한 윈도우의 세션 state에 라우팅된다. 전역 pending request table을 윈도우들이 공유하면 한 윈도우의 cleanup이 다른 윈도우의 approval/RPC를 미아로 만들어 1.5(approval deadlock)와 결합한다.
- **완화**:
  1. registry는 **단일 윈도우가 소유**하고 `runtimeId`↔window 바인딩을 명시한다. window-close 시 **자기 소유 엔트리만** 정리하고 타 윈도우 엔트리는 건드리지 않는다.
  2. **cross-window dispatch 금지(불변식)**: event/RPC는 소유 윈도우 안에서만 라우팅한다. 다른 윈도우의 `runtimeId`/sessionHandle로 dispatch 시도는 차단한다.
  3. 윈도우 소유권/수명 모델의 정본은 12 T1.4이며, 본 위험은 그것을 인용한다. 모델은 OQ-48에서 확정됐다.
- **검출**: `agent-event-router.test.ts`가 window-close 시 타 윈도우 `runtimeId`/store 보존, cross-window dispatch 차단, 동일 JSON-RPC id를 쓰는 두 runtime의 pending approval 격리를 검증한다. cross-window dispatch 시도 카운터/telemetry는 운영 관측 필요가 생기면 후속으로 추가한다.

### 1.12 Long-session transcript memory (S2)

긴 세션에서 frontend in-memory `TranscriptModel`(08 §5)이 unbounded로 증가한다. DOM 가상화(OQ-17)는 렌더되는 **DOM만** bounded로 묶을 뿐, 반응형 store와 item body Map은 세션 길이에 비례해 계속 자란다. §1.8(transport/backpressure/emit, backend replay log)과는 **분리된** frontend heap/model growth 문제다(이건 backend가 아니라 frontend 메모리 표면).

- **영향**: 수백~수천 turn 세션에서 (a) 반응형 표면이 전체 item에 걸려 streaming마다 반응성 오버헤드가 세션 길이에 비례해 커지고, (b) item body(diff/이미지/명령 출력 포함)가 heap에 무한 누적되어 long-running 윈도우가 OOM·렉으로 무너진다. DOM 가상화만으로는 surface(반응형 모델/heap)가 줄지 않아(OQ-17과 별개) 가시 영역이 작아도 메모리는 계속 증가한다.
- **완화**:
  1. **shallow 반응형 표면**: 반응형(`$state`)은 `visibleItemIds`/`itemVersions`/`status`/`pending`만 보유하고 item body는 plain `Map`(`itemsById`)에 둔다. streaming은 body 갱신 + 해당 `itemVersion` bump로 처리해 반응성 오버헤드를 세션 길이와 무관하게 bounded로 만든다(08 §5 view-model, 04 §3.7 reducer). DOM 가상화(OQ-17)와 별개로 **surface가 살아 있어도 heap이 줄어든다**.
  2. **sealed-turn 윈도우 eviction**: 윈도우 = 최근 N개 **sealed** turn + 모든 unsealed/active turn(인터리빙 포함) + streaming. cap 초과 시 oldest sealed turn body를 evict해 heap을 bounded로 유지한다(04 §3.7 seal 조건·불변식, 08 §5 `turnsById`/`tombstones`).
  3. **3-상태 turn residency**(`TranscriptTurnResidency`, 08 §5 정의): `unsealed`(모든 event apply) / `sealed-retained`(body 유지, 늦은 event→unseal→patch→reseal+telemetry) / `evicted-tombstone`(body 없음, 늦은 event **apply 금지**+`droppedLateEventCount`, tombstone은 작은 LRU/TTL로 bounded). late-event 규칙·seal 불변식의 정본은 04 §3.7, view-model 타입(`TranscriptModel`/`TranscriptTurnResidency`)의 정본은 08 §5다.
  4. **격리 replay-reload**: tombstone 구간 스크롤백은 read-only history inspection으로만 복구한다 — "이전 기록 불러오기" 시 `canLoad`면 격리 scratch replay 세션(session/load·thread/read)으로 조회하고 live store에 미병합, 아니면 "사용 불가" notice(10 §4.7 runtime scrollback replay, 12 T5.6). 복원/영속 캐시가 아니다(RD-3 transcript full cache 제외와 정합).
  5. frontend 전용 완화다. backend(07)·persistence(10: metadata-only + replay-first)는 무변경이며 cold/tombstone 구간은 **runtime-only(미영속)**이다.
- **검출**: long-session fixture(수백~수천 turn)에서 (a) 반응형 표면 크기(`visibleItemIds`/`itemVersions`)가 윈도우 cap에 수렴하는지, (b) evict 후 heap이 cap 이하로 유지되는지, (c) tombstone 구간 late-event가 apply되지 않고 `droppedLateEventCount`만 증가하는지, (d) sealed-retained로 흡수되는 늦은 same-turn event가 unseal→patch→reseal 경로로 처리되는지 assert(11 late same-turn fixture = reducer/store event fixture). window/cap 수치 실측은 OQ-52.

### 1.13 Auto-follow DOM 효과 / 가상화 상호작용 (S3)

transcript의 바닥 추종(auto-follow)은 상태 토글이 아니라 스크롤 컨테이너(`.transcript-region`) DOM 부수효과로 명세된다(08 §3). 가상화(08 §7.2)가 도입되면 추종 로직과 item mount/unmount가 상호작용해 스크롤 위치가 잘못 점프할 수 있다(공유 결정 #4).

- **영향**: autoFollow & 바닥-anchored 상태에서 새 item/delta마다 `scrollTop=scrollHeight`로 추종하는데, 가상화가 바닥의 streaming item(또는 바닥 근처 item)을 unmount하면 `scrollHeight`가 갑자기 줄어 추종 점프가 어긋난다(바닥에서 튀거나 스크롤이 중간으로 빨려 들어감). 고빈도 delta에서 매 이벤트 동기 `scrollTop` 쓰기를 하면 레이아웃 thrash로 렌더가 끊긴다. approval이 가상화 윈도우 밖이면 `scrollIntoView`가 빈 슬롯을 가리킨다.
- **완화**:
  1. auto-follow는 DOM 부수효과로 구현하되, autoFollow & 바닥-anchored일 때만 `scrollTop=scrollHeight`를 적용하고 `onscroll`로 추종 on/off를 토글한다. 고빈도 delta는 `tick()`/rAF로 디바운스해 레이아웃 thrash를 막는다(08 §3 정본).
  2. **가상화 always-mount 게이트(불변식)**: 가상화 도입 시 `streamingItemId` + 바닥 N개 item을 **항상 mount**해 unmount로 인한 `scrollHeight` 점프를 차단한다(08 §7.2). approval은 항상 `scrollIntoView` 대상이 되도록 mount 보장.
  3. 정본은 08 §3(auto-follow DOM 효과)·§7.1·§7.2(가상화)이며 본 위험은 그것을 인용한다. 상호작용 확정은 OQ-58 게이트. §1.12(transcript memory/가상화 surface)와는 **분리된** 스크롤 추종 정합 문제다.
- **검출**: streaming 중 바닥 item unmount→`scrollHeight` 점프가 발생하지 않는지 회귀(11 §). 고빈도 delta에서 추종이 바닥에 안정적으로 anchored되는지, 가상화 윈도우 밖 approval이 mount되어 `scrollIntoView`가 동작하는지 assert.

---

## 2. Resolved defaults (확정 기본값)

아래는 v1 구현이 **그대로 따르는** 확정 기본값이다. 변경하려면 이 문서를 갱신하고 관련 문서(05/06/07/10/15)에 반영해야 한다.

| # | 결정 | 값 / 정책 | 근거 |
|---|---|---|---|
| RD-1 | runtime gating | direct runtime은 첫 구현에서 **실험 flag 뒤**에 둔다. experimental Codex 기능은 그 안에서 한 단계 더 gate. | 위험 1.2 |
| RD-2 | transport 우선순위 | Codex·Claude 모두 **stdio 우선**. websocket은 후속(검증 후 optional), 1차 handler는 명시적 거부. v1은 `websocket` start를 **로깅/스냅샷 노출 전 reject**하고, `authToken`은 09 redaction 집합에 포함한다 — token 필드가 15 §8.1 계약에 존재하나 v1 미사용·누출 차단(variant 타입 자체는 future-sketch로 유지). | 05 §1, 07 §transport, 09(redaction 집합), 위험 1.3, 15 §8.1 |
| RD-3 | transcript full cache | 1차 범위에서 **제외**. provider replay(ACP `session/load`, Codex `thread/read`)와 metadata 저장에 집중. | [`10-persistence-migration.md`](10-persistence-migration.md) §40, 15 §7 |
| RD-4 | raw protocol log | **기본 off**. redacted debug mode만 둔다(비밀·자격 redaction 적용). | 09 §, 위험 1.1 |
| RD-5 | Claude adapter 의존성 | npm `@agentclientprotocol/claude-agent-acp@**0.51.0**` 고정(commit `23626c9`). | 15 §0(헤더), ref-claude, 위험 1.10 |
| RD-6 | Codex ref 핀 | baseline은 `rust-v0.142.0` 기준 매핑. 구현 시점 CLI/generated schema가 다르면 OQ-41 절차로 diff 확인 후 핀 갱신 여부 결정. | 15 §0(헤더), ref-codex, 01 §4 |
| RD-7 | ACP 기준 artifact | 구현 wire target은 `protocolVersion=1`. v1 구현은 `@agentclientprotocol/claude-agent-acp@0.51.0`이 고정하는 `@agentclientprotocol/sdk@0.29.0` package schema/dist와 `contracts/claude-acp.ts` 부분 wire mirror(13종 `session/update`)를 기준으로 한다. baseline 후보 `schema-v1.16.0`은 구현 핀이 아니며, 의존성 refresh 시 OQ-41 절차로 다시 대조한다. | 15 §0(헤더), ref-acp, 01 §4, impl-log T0.0/OQ-41 |
| RD-8 | startup allowlist 강화 (R4 정본) | executable `command`는 renderer 입력에서 제거하고 backend가 `provider`로 신뢰 절대경로를 resolve한다. `args`/`env`는 adapter가 생성한 검증값만 허용하며 Rust handler가 **provider별 allowlist로 재검증**(임의 executable/shell string 차단). **basename-only 검사는 폐기**하고 다음으로 강화: (a) executable은 backend가 resolve한 신뢰 절대경로 또는 사전 등록된 절대경로 화이트리스트만 사용. (b) **args 정확 일치**: Codex는 `["app-server","--stdio"]` 정확히, Claude는 `args.length==2` 이고 `args[0]`이 검증된 `adapterEntryPath`(절대경로, `claude-agent-acp` `dist/index.js` 패턴), `args[1]`이 고정 `--hide-claude-auth`일 것. 임의 `.js`/임의 바이너리/임의 argv 거부. (c) **env key allowlist**: key는 `^[A-Za-z_][A-Za-z0-9_]*$` + OQ-38에서 확정한 provider별 허용 key 집합으로 강제하고 값은 non-secret(C1/RD-14). shell metachar 검사는 방어용으로 유지. PTY 대비 의도적 강화. | 위험 1.2, OQ-34, 07 §8.1, 09(untrusted renderer 위협모델), 11(allowlist 테스트), 15 §8.1 주석, `research/codebase-backend.md` §6·§10 권고 6 |
| RD-9 | resume 비밀 scrub | `providerSessionId`/`providerThreadId`/`providerResumeToken`은 디스크 저장 직전 scrub(기존 `pty_id`/`resume_token`과 동일). history 미저장. | 15 §7.3 보안 경계, `research/codebase-backend.md` §4.2·§4.4·§10 권고 7 |
| RD-10 | unknown variant 처리 | adapter는 unknown method/variant를 **silent drop 금지**. `raw` 보존 + 로그/카운터. | 위험 1.1, 15 §0.2 |
| RD-11 | pending approval cleanup | turn cancel/shutdown(process 생존) 시 unresolved approval을 `cancelled`로 닫고 wire 응답 전송. process exit(사망) 시 approval은 `failed`(wire 미전송, 내부 전용), pending RPC는 failed. | 04 §4.2·§5.0, 위험 1.5 |
| RD-12 | node 설치 전제 | Claude adapter는 node 런타임 + SDK **optional dependency 포함** 설치를 전제. `--omit=optional` 금지, startup preflight 수행. | ref-claude §node, 위험 1.9 |
| RD-13 | thought / audio content | **(해소됨, D11/OQ-04)** thought/reasoning은 `agent_message`/`agent_message_delta`의 optional `channel?: "response" \| "thought"`(미지정 시 `"response"`)로 normalize한다(15 §3). thought delta는 messageId/contentIndex별 append, completed reasoning item이 thought 채널의 권위(reconcile)다(04 §3.2.2·§3.2.5). UI는 `channel==="thought"`를 접이식 thinking 블록(기본 collapsed)으로 렌더한다(08). `audio` content는 v1 미지원이며 composer capability는 false, inbound ACP `audio`는 drop하지 않고 `AgentContent{type:"json"}` raw 블록으로 보존한다. | 15 §3·§4, 04 §3.2.2·§3.2.5, 05 §5.2, 06 §5·§7.2, 08, OQ-01/OQ-04 |
| RD-14 | secret env 경계 (C1, R1 — 06까지 통일) | `AgentRuntimeStartParams.env`(15 §8.1) 및 06 `ClaudeAcpLaunchConfig.env`/`buildClaudeAcpLaunchParams`의 `env`는 **non-secret 전용**. secret(API key/OAuth token/gateway header/session cookie)은 launch argv(`-e env KEY=VAL`)로 절대 넘기지 않고, 필요 시 `std::process::Command::env()`+`WSLENV` passthrough(argv 비경유)로만 전달한다. v1 기본은 provider 자체 WSL 인증(`claude login`/`codex auth`)에 의존해 secret 미전달. argv는 OS 관측면(`ps`/`/proc/<pid>/cmdline`/WSL process 목록)에 평문 노출되어 redaction으로 막을 수 없다. | OQ-28, 06 §2.2·§3.3, 07 §5.1·§11, 09, 15 §8.1 주석(타입 비변경) |
| RD-15 | 미지원 server request 응답 의무 (R5) | provider server→client **REQUEST**(JSON-RPC `id` 있는 요청) 중 미지원 method는 **반드시 JSON-RPC error 응답**(예: `code -32601` method not found) 또는 **명시적 decline/cancel 응답**을 보낸다. **무응답 silent-drop 금지**(provider 영구 대기/deadlock 방지). unknown **NOTIFICATION**(`id` 없음)은 응답 불필요 — `raw` 보존 + counter로 가시화(RD-10). adapter default 분기는 `routing.resolveApproval` 후 `return []`로 끝내지 말고 unknown server request에 error/unsupported(또는 decline) 응답을 먼저 보낸다. | 위험 1.5·OQ-35, 04 §5, 05 §7.1, 06, [`ref-acp-protocol.md`](ref-acp-protocol.md) §1·§"-32601", [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) §4·§"-32601", RD-10 |

> 기존 문서(보강 전)에 있던 5개 default(실험 flag, stdio 우선, transcript cache 제외, raw log off, npm 고정)는 모두 위 표에 흡수·구체화했다. npm 버전은 **0.50.0 → 0.51.0**으로 갱신(RD-5). 이후 Codex 재검토(R1~R5)로 RD-8(allowlist 강화)을 구체화하고 RD-14(secret env 경계, R1)·RD-15(미지원 server request 응답 의무, R5)를 신설했다.

---

## 3. Open questions 레지스트리

다른 문서에서 "unverified" 또는 "결정 필요"로 올라온 항목을 한 곳에 집계한다. 구현 에이전트는 **해당 항목을 건드리기 전에** "구현 전 확인 방법"을 먼저 수행한다.

> **H4 — "Codex wire 실측" 묶음(해소됨)**: **OQ-33**(`UserInput.text` vs `text_elements`)·**OQ-07**(`initialize`→`initialized` 핸드셰이크)·**OQ-11**(`ClientInfo`/`InitializeCapabilities` 필드)은 T0.0 직후 `codex app-server generate-ts` 산출물(0.142.2)과 최소 wire 시퀀스 검토로 처리됐다. `UserInput.text`는 `text_elements` 필수, initialize는 방어적 기본값으로 항상 수행, `ClientInfo`/`InitializeParams`/`InitializeCapabilities` 생성 타입은 존재하므로 최소 client info + 보수적 capabilities로 구현한다. 이전 hard gate는 해제됐고 05 §initialize/§5.3d는 이 결정값을 따른다.

| ID | 질문 | 출처 문서 §| 현재 기본값(가정) | 구현 전 확인 방법 |
|---|---|---|---|---|
| OQ-01 | **(해소됨, D11)** `agent_thought_chunk`(ACP thought) / Codex `reasoning`에 대응하는 전용 event/content가 없다 — v1 처리 정책? | 15 §3, [`ref-acp-protocol.md`](ref-acp-protocol.md) §13.2·§14, [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) §6.3 | **확정**: `agent_message`/`agent_message_delta`에 optional `channel?: "response"\|"thought"` 추가(15 §3). thought delta는 messageId/contentIndex별 append, completed reasoning item이 권위(04 §3.2.2·§3.2.5). 05 §5.2: `item/reasoning/textDelta`→`agent_message_delta{channel:"thought"}`, completed reasoning item→`agent_message{channel:"thought", mode:"replace"}`(더 이상 `[]` 드롭 아님). 06 §5: `agent_thought_chunk`→`agent_message_delta/agent_message{channel:"thought"}`. | 해소 완료 — 다운스트림은 위 정책을 그대로 구현. UI 렌더(접이식 thinking 블록, 기본 collapsed)는 08 §5.1에서 확정. |
| OQ-02 | **(해소됨)** `TokenUsage` vs ACP `usage_update`(`used`/`size`/`cost`) — context gauge 표현에 04/15 필드 추가가 필요한가? | [`research/ux-reference.md`](research/ux-reference.md) §12·§266, 15 §5 `TokenUsage` | **확정**: ACP `UsageUpdate{used,size}`를 `TokenUsage.contextUsed`/`contextSize`로 매핑한다(15 §5 2필드 추가, 06 §3.6 매핑). Codex 축(input/output/cached/reasoning)과 별개로 context gauge 전용 필드다. | 해소 완료 — 15 §5에 `contextUsed?`/`contextSize?` 추가, 06 §3.6에서 `usage_update`→두 필드 매핑. 08 context gauge는 이 두 필드를 읽는다. |
| OQ-03 | **(해소됨, OQ-45 연동)** ACP `Diff{oldText,newText}` → 15 `AgentContent.diff{patch}` 변환 규칙 | [`research/ux-reference.md`](research/ux-reference.md) §12, 15 §4, [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md) §7.2 | **확정/구현**: ACP `ToolCallContent{type:"diff", path, oldText, newText}`를 `AgentContent{type:"diff", path, patch}`로 정규화하고, adapter가 oldText/newText로 unified patch를 생성한다. `oldText=null`은 신규 파일(`/dev/null`)로 표시한다. `@agentclientprotocol/claude-agent-acp@0.51.0` dist artifact의 `Write`/`Edit`/`structuredPatch` 출력 shape와 정합한다. | 해소 완료 — `claude-acp-content.test.ts`가 diff→patch와 `oldText=null` 신규 파일을 검증한다. dist `toolUpdateFromDiffToolResponse`가 여러 structuredPatch entry를 여러 diff content로 내보내므로, adapter는 각 content를 독립 diff block으로 표시한다. |
| OQ-04 | **(해소됨)** ACP `audio` content 미지원 결정 시 강등/표시 정책? | [`ref-acp-protocol.md`](ref-acp-protocol.md) §13.2·§14, [`research/ux-reference.md`](research/ux-reference.md) §12 | **확정**: v1은 audio composer capability를 false로 둔다. inbound `audio` content는 drop/무시하지 않고 `AgentContent{type:"json", value:<raw audio block>}`로 보존해 transcript/tool content의 raw JSON 블록으로 표시한다. text delta 경로로 새지 않는다. | 해소 완료 — `claude-acp-content.test.ts`와 `claude-acp-session-update.test.ts` CL-18이 audio→json raw 보존·crash 없음·text delta 미유출을 검증한다. |
| OQ-05 | **(해소됨)** send/개행 키 바인딩 — `Enter`=전송 vs `Shift+Enter`=전송? | [`research/ux-reference.md`](research/ux-reference.md) §370·§428 | **확정/구현**: `Enter`=전송, `Shift+Enter`=개행(비전송). slash command palette가 열려 있을 때만 `Enter`/`Tab`은 command 선택으로 소비되고 전송하지 않는다. | 해소 완료 — `AgentComposer.svelte` `onKeydown`과 `AgentComposer.test.ts`가 Enter 전송, Shift+Enter 비전송, command palette Enter 선택 비전송을 검증한다. |
| OQ-06 | **(해소됨)** session/thread 목록 UI — Zed식 sidebar/switcher 도입 vs 기존 탭 모델 + turn status badge? | [`research/ux-reference.md`](research/ux-reference.md) §429 | **확정/구현**: v1은 기존 CLCOMX 탭 모델을 유지하고 `runtimeKind`로 PTY/direct host를 분기한다. Zed식 sidebar/switcher는 도입하지 않는다. direct runtime status는 `AgentTranscriptSurface`의 store status에서 `onAgentRuntimeStatusChange`로 publish되고, `SessionShell`/`SessionViewport`/`App`을 거쳐 live session의 `agentRuntimeStatus?: AgentSessionStatus`에만 저장된다. `SessionTabViewModel`은 이 필드를 받아 `TabBar.svelte`에 i18n label/tooltip이 있는 작은 status indicator를 렌더한다. workspace snapshot에는 저장하지 않는다. | 해소 완료 — `TabBar.test.ts` OQ-06, `session-store-mutations.test.ts` OQ-06, `session-shell-adapter.test.ts`, `SessionViewport.test.ts`, `AgentTranscriptSurface.test.ts` OQ-06이 status publish→live session→tab badge 경로를 검증한다. |
| OQ-07 | **(해소됨, H4 기본값 유지)** Codex `initialize`→`initialized` 핸드셰이크가 필수인가(없이 `thread/start` 가능?), 메시지 순서/타이밍 검증 | [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) §10·§606·§613·§614 | **확정**: `initialize`→`initialized` 핸드셰이크를 **항상 수행**한다(방어적 기본값). | 해소 완료 — H4 묶음에서 기본값을 유지했고 05 startup 시퀀스와 adapter 구현은 initialize 후 thread/turn 경로를 사용한다. |
| OQ-08 | **(해소됨, exact 0.51.0 pin + package schema/dist 기준)** claude-agent-acp `0.50.0` vs `0.51.0` capability 차이(diff 미인용) | [`research/ux-reference.md`](research/ux-reference.md) §430, [`ref-claude-agent-acp.md`](ref-claude-agent-acp.md) §6·§488 | **확정/구현**: v1은 `0.50.0` 조사값을 더 이상 기준으로 삼지 않는다. `package.json`/`package-lock.json`은 `@agentclientprotocol/claude-agent-acp`를 exact `0.51.0`으로 고정하고, 해당 package는 `@agentclientprotocol/sdk@0.29.0`, `@anthropic-ai/claude-agent-sdk@0.3.187`, node `>=22`, bin `dist/index.js`를 고정한다. 런타임 wire 기준은 `protocolVersion=1` + SDK 0.29.0 package schema/dist + CLCOMX 부분 wire mirror다. | 해소 완료 — `node_modules/@agentclientprotocol/claude-agent-acp/dist/acp-agent.js`의 initialize result는 `promptCapabilities.image/embeddedContext=true`, `loadSession=true`, `sessionCapabilities.resume={}`를 광고하고, SDK schema는 `plan_update`/`plan_removed` 포함 13종 `SessionUpdate`를 정의한다. `claude-acp-initialize.test.ts`와 fixture replay가 이 capability 파싱을 고정한다. 0.50.0 CHANGELOG 비교는 현 v1 구현 기준 결정에 필요하지 않으며, 향후 dependency refresh 때만 OQ-41 절차로 schema diff + fixture replay를 반복한다. |
| OQ-09 | **(해소됨, 2026-06-28 공식 overview 확인)** Anthropic이 서드파티 제품에 claude.ai 로그인/rate limit 제공을 허용하는가 | [`ref-claude-agent-acp.md`](ref-claude-agent-acp.md) §1·§6·§482, [`09-permissions-security.md`](09-permissions-security.md) §9 | **확정**: 공식 Agent SDK overview는 third-party products가 Claude app credentials/rate limits를 제공하는 것을 허용하지 않고 API key 사용을 권장한다. branding guidelines는 Anthropic이 만든/후원/보증한 제품처럼 암시하지 말라고 요구한다. v1은 API key/기존 WSL 측 자격 전제, terminal/gateway auth capability 미광고, `--hide-claude-auth`, 중립 provider label을 유지한다. **후속(2026-07-05)**: 개인 사용 opt-in `agentRuntime.claudeAllowSubscriptionAuth` 설정(기본 false)이 추가됐다 — 켜면 `--hide-claude-auth`를 생략해 기존 WSL 측 구독 로그인을 direct에서 재사용한다(09 §9, 앱 내 로그인 flow는 여전히 미노출). | 해소 완료 — 09 §9와 01 §4.4에 공식 근거를 반영했다. 출시/배포 전에는 같은 공식 overview/terms를 재확인하고, 구독 opt-in의 배포 적합성도 함께 재확인한다. |
| OQ-10 | **(부분 해소, startup native preflight 구현 + 현재 로컬 WSL target 실측 완료 / 배포 matrix 잔여)** 배포 대상의 node/claude/SDK 번들 버전(조사 머신 값과 다를 수 있음) | [`ref-claude-agent-acp.md`](ref-claude-agent-acp.md) §6·§119·§485 | **구현 하한**: start 직전 node `--version` preflight와 Claude native `node <adapterEntryPath> --cli --version` probe를 실행한다. native probe는 검증된 launch env(`CLAUDE_CODE_EXECUTABLE` 등)를 받아 실제 launch와 같은 env 조건에서 SDK optional dependency 또는 override resolve를 확인한다. **현재 로컬 WSL target 실측(2026-06-30)**: Ubuntu 24.04.4 LTS / WSL2 `6.18.33.2-microsoft-standard-WSL2`, node `v24.11.1`(`/home/xenia/.nvm/versions/node/v24.11.1/bin/node`), `@agentclientprotocol/claude-agent-acp@0.51.0`, transitive `@agentclientprotocol/sdk@0.29.0`, `node node_modules/@agentclientprotocol/claude-agent-acp/dist/index.js --cli --version` → `2.1.187 (Claude Code)`, `CLAUDE_CODE_EXECUTABLE`/`CLAUDE_CONFIG_DIR` 미설정. | 부분 해소 완료 — RS-10f/10g/10h/10i/10k/10l/10m이 node/native preflight와 실패 차단을 검증하고, 현재 로컬 WSL target에서는 native binary resolve까지 확인했다. 남은 범위는 릴리스/설치 배포 matrix에서 node/claude/SDK 버전과 OS/WSL 조합을 재확인하고, 필요하면 version range 정책을 강화할지 결정하는 것이다. |
| OQ-11 | **(해소됨, H4)** Codex `ClientInfo`/`InitializeCapabilities` 정확 필드(capability opt-in 키, experimental 노출 제어) | [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) §10·§148 | **확정**: `codex app-server generate-ts` 산출물에 `ClientInfo`/`InitializeParams`/`InitializeCapabilities` 타입이 존재한다. v1은 최소 `ClientInfo`(`name`/`version`)와 보수적 capabilities를 전송하고 experimental 키는 사용하지 않는다. | 해소 완료 — 05 §initialize와 adapter 구현은 최소 client info + 보수적 capabilities 기본값을 따른다. capability opt-in 확장은 후속 기능별로 별도 검증한다. |
| OQ-12 | **(해소됨, v1 data URI)** ACP image: base64 `data` → 15 `AgentContent.image.uri` 변환(data URI vs 저장 후 uri) | 15 §4 주석, [`ref-acp-protocol.md`](ref-acp-protocol.md) §13.2 | **확정/구현**: v1은 저장소에 이미지 blob을 쓰지 않고, inbound ACP `image{data,mimeType}`를 `AgentContent{type:"image", uri:"data:<mime>;base64,<data>", mimeType}`로 보존한다. provider가 `uri`를 함께 주면 `uri`를 우선 사용한다. composer outbound는 data URI에서 base64를 다시 추출해 ACP `image{data,mimeType}`로 보낸다. | 해소 완료 — `claude-acp-content.test.ts`가 inbound image base64→data URI, `uri` 우선, outbound data URI→ACP image 변환을 검증한다. 대용량 image data URI의 heap/표현 한도는 저장 정책 재오픈이 아니라 OQ-52 heavy-item/image cap 실측 범위에서 조정한다. |
| OQ-13 | **(해소됨)** ACP `requires_action`/`running`/`idle` 합성 규칙(특히 permission pending ↔ `requires_action`) 확정 | [`ref-acp-protocol.md`](ref-acp-protocol.md) §13.1·§695, 04 §2.2 | **확정/구현**: ACP는 permission pending 상태 wire가 별도 status로 오지 않으므로 client가 합성한다. `session/prompt` 전송 시 `session_status_changed:running`, `session/request_permission` 수신 시 `approval_requested`와 함께 `session_status_changed:requires_action`, permission resolve 후 pending이 없으면 `running` 복귀, `session/prompt` response `stopReason` 수신 시 `turn_completed` + `session_status_changed:idle`이다. `turn_completed{failed}`도 세션 failure가 아니라 `idle`이다. | 해소 완료 — `claude-acp-adapter.test.ts` CL-7/CL-8/CL-19가 ACP prompt/permission/idle 합성을 검증하고, `agent-runtime-store.svelte.test.ts` NM-24/NM-25/NM-26 및 NM-12/13이 store 상태 전이를 고정한다. |
| OQ-14 | **(해소됨)** Codex `reject_always` 등가 영구 거부 decision의 정확한 매핑 | [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) §10·§615, generated `CommandExecutionApprovalDecision`/`FileChangeApprovalDecision` | **확정/구현**: Codex decision enum에는 영구 거부 값이 없으므로 공용 approval 경계의 `reject_always`는 `decline`으로 방어 매핑한다. v1 Codex command/fileChange UI는 `reject_always`를 노출하지 않는다. | 해소 완료 — `OPTION_KIND_TO_DECISION.reject_always = "decline"` 및 `codex-app-server-adapter.test.ts` CX-14가 `reject_always → {decision:"decline"}`을 검증한다. |
| OQ-15 | **(해소됨, v1 reject-before-log gate)** Codex websocket transport·auth token 저장/redaction 정책 | 05 §12·§72, 07 §transport, 09 §5, 11 RS-12c/RS-18, 15 §8.1 | **확정/구현**: v1은 `jsonrpc-stdio` 전용이다. `transportKind:"websocket"`은 spawn·snapshot·params logging 전에 정적 에러 `only jsonrpc-stdio transport is supported in v1`로 거부하고, `authToken`은 09 secret scrub/redaction 집합에 포함한다. variant 타입은 future-sketch로 유지한다. | 해소 완료 — `allowlist::validate_and_extract`와 test-mode mock provider가 websocket variant를 params 직렬화 없이 거부하고, `agent_runtime::tests` RS-12c/RS-18이 오류 문자열에 token 값이 들어가지 않음을 검증한다. 실제 websocket wire/framing/auth 저장 정책은 websocket 구현을 시작하는 후속 scope에서 다시 연다. |
| OQ-16 | **(v1 해소 후 후속 구현됨)** direct 세션 cross-restart 복원 — scrub로 재개 키가 디스크에 없어 앱 재시작 후 direct 대화 복원 불가. 허용할 것인가/대안? | [`10-persistence-migration.md`](10-persistence-migration.md) §4.4·§4.4a·§4.5·§6, 15 §7.3, 11 FE-24b/FE-24c/E2E-12/E2E-13a/E2E-13b, [`oq16-cross-restart-restore-design.md`](oq16-cross-restart-restore-design.md), [`oq16-cross-restart-restore-plan.md`](oq16-cross-restart-restore-plan.md) | **v1 확정(당시)**: v1은 앱 재시작 후 direct 대화 복원을 지원하지 않았다. `providerSessionId`/`providerThreadId`/`providerResumeToken`은 backend 영속화 직전 scrub되고, scrub된 cold-restore metadata는 `resumeSession`을 호출하지 않고 새 direct session으로 시작하되 "이전 대화를 복원할 수 없음" notice를 표시했다. provider id가 있어 resume/load를 시도했지만 실패한 경우도 실패 controller를 정리하고 fresh start + 같은 notice로 낮췄다. **후속 구현(하이브리드 복원)**: v1이 남겨둔 "복원을 원하면 재개 키를 OS secret store에 저장하거나 provider session store에 의존"을 그대로 채택해 구현했다. (1) 암호화 재개 id 저장소(`src-tauri/src/features/agent_runtime/secret_store.rs`) — OS 키스토어(`keyring` crate)의 단일 앱 키 1개 + AES-256-GCM으로 세션별 `providerThreadId`/`providerSessionId`/`canResume`/`canLoad`를 at-rest 암호화 저장(`providerResumeToken`은 계속 미저장). (2) bounded transcript 캐시(`transcript_cache.rs` + `service/transcript-cache.ts`) — residency 윈도우 범위로 bound, tombstone 제외, redaction 적용. (3) `AgentTranscriptSurface.svelte`가 cold restart 시 캐시를 **즉시 read-only 렌더**하고, 실제 resume/process spawn은 **탭 포커스(`props.visible` 최초 true) 시점으로 지연**한다(다중 세션 동시 복원 AppHang 대비). resume이 권위 replay로 성공하면 캐시를 비우고 replay로 대체하며, 미지원/id 없음이면 캐시를 `historyReadOnly` read-only 히스토리로 유지하되 **그 아래 fresh 세션이 정상 활성화**되어 이어갈 수 있다(composer 잠금 아님). resume RPC 실패는 기존 v1 fallback(fresh start + 복원 불가 notice)을 그대로 따른다. `workspace.json` scrub 경계(§6)는 변경 없이 유지된다. | v1 해소 근거 — `AgentTranscriptSurface.test.ts` FE-24b/FE-24c가 scrub metadata와 resume 실패 경로를 검증하고, `session-store-snapshot.test.ts`가 direct metadata scrub 후 저장→복원 경계를, Rust `workspace::store::tests`가 provider id/resume token 디스크 scrub을 검증한다. `e2e/agent-runtime/agent-runtime.test.ts` E2E-12는 scrubbed direct `workspace.json` seed가 Windows app boot에서 fresh direct host + 복원 불가 notice + 새 prompt 응답으로 이어지는지 검증한다. **후속 구현 근거** — Rust 단위 테스트가 암호화 저장소 암·복호화 왕복/키 부재·손상 시 graceful 폴백/`providerResumeToken` 미저장을 검증하고, frontend 단위 테스트가 TranscriptModel 직렬화 왕복·residency bound·redaction을 검증한다. `e2e/agent-runtime/agent-runtime.test.ts`에 E2E-13a(캐시 즉시 렌더 + resume 성공 이어가기)/E2E-13b(resume 미지원 시 read-only 히스토리 + fresh 세션 composer 사용 가능)를 추가했고 **2026-07-05 Windows E2E 러너에서 agent-runtime pack 20/20 통과**했다 — E2E-13a는 test-mode 고정 앱 키(debug 빌드 한정 `#[cfg(debug_assertions)]`)로 암호화 저장소 파일을 직접 시드해 TB-5 복호화 → resume 경로를 app-boundary에서 검증한다. |
| OQ-17 | **(해소됨, v1 surface unmount 회피)** message seq/delta 후속 단계 범위 / late-attach seq 메커니즘 부재 — process 생존 중 transcript surface unmount 시 store 재구성 불가 | [`10-persistence-migration.md`](10-persistence-migration.md) §4.3, 15 §8.3, [`research/codebase-backend.md`](research/codebase-backend.md) §2.3·§10, [`08-ui-composition.md`](08-ui-composition.md) §2.2·§7.2 | **확정/구현**: v1은 transcript surface를 process 생존 동안 unmount하지 않는 방식으로 late-attach 재구성 공백을 회피한다. `SessionViewport`는 모든 keyed session shell을 유지하고 `visible` prop만 바꾸며, `AgentTranscriptSurface`는 `visible=false`를 `.hidden` CSS로만 처리해 runtime subscribe/store를 유지한다. DOM 가상화는 v1 미도입이고, residency eviction은 surface/store 유지와 별개로 heap만 줄인다. message `seq`/snapshot/delta-since는 후속이다. | 해소 완료 — `SessionViewport.test.ts` FE-21이 activeSessionId 변경 시 shell destroy/remount 없이 `visible`만 바뀜을 검증하고, `AgentTranscriptSurface.test.ts` OQ-17이 direct host visible toggle에서 `.hidden`만 토글되고 `port.shutdown`이 호출되지 않음을 검증한다. Windows `agent-runtime` E2E-9도 direct+PTY 탭 전환 중 direct host DOM 유지 spec으로 통과했다. seq 도입 시 message payload에 단조 증가 seq + snapshot/delta-since를 추가해 late-attach 재구성을 별도 활성화한다. |
| OQ-18 | **(해소됨, persistence/FE-22..24)** `WorkspaceTabSnapshot` 확장(`runtime_kind`/`agent_runtime`)의 동기화 범위 — `merge_workspace_snapshot`/`applyWorkspaceWindowSnapshot`/`session-store-snapshot.ts` 영향 | [`10-persistence-migration.md`](10-persistence-migration.md) §3.2·§3.3, [`research/codebase-backend.md`](research/codebase-backend.md) §11, [`research/codebase-frontend.md`](research/codebase-frontend.md) §10 | **확정/구현**: TS 저장 `session-store-snapshot.ts::createWorkspaceTabSnapshot`, 복원 `live-session-workspace-sync.ts::createSessionCore`/`createRuntimeSession`, 기존 세션 갱신 `applyWorkspaceWindowSnapshot`가 `runtimeKind`/`agentRuntime`를 전파한다. Rust `WorkspaceTabSnapshot`은 `runtime_kind`/`agent_runtime` 필드를 가지고, `merge_workspace_snapshot`는 incoming tab metadata를 보존한다. | 해소 완료 — frontend round-trip/기존 세션 갱신 테스트와 Rust `merge_workspace_snapshot_preserves_direct_runtime_metadata`, `legacy_workspace_without_runtime_fields_defaults_to_pty`, scrub 테스트가 10 §3/§6 계약을 검증한다. |
| OQ-19 | **(해소됨, D12 v1)** Codex permissions approval(`item/permissions/requestApproval`) 응답 `{permissions: GrantedPermissionProfile, scope}` 구성 | [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md) §7.1·§7.2, [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) §4.3, generated `PermissionsRequestApprovalResponse` | **확정/구현**: v1은 command/fileChange 승인만 지원한다. permission-profile escalation request는 사용자에게 노출하지 않고 `{permissions:{}, scope:"turn"}`로 자동 decline하며 원본 request는 `ProviderRef.raw`에 보존한다. | 해소 완료 — `mapCodexServerRequest("item/permissions/requestApproval")`가 `auto-decline` reply와 `approval_resolved{failed}`를 만들고, `codex-app-server-adapter.test.ts`가 원본 JSON-RPC id 타입을 보존한 `{permissions:{}, scope:"turn"}` 응답을 검증한다. `GrantedPermissionProfile`을 사용자 승인 UI로 구성하는 정식 permission grant 지원은 후속 기능 범위다. |
| OQ-20 | **(해소됨, v1 stable params)** `turn/start` approval/sandbox 정책 필드(`approvalPolicy`/`sandboxPolicy`/`model`/`effort`) v1 노출 여부·기본값 | [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md) §2.4, [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) §6.5 | **확정/구현**: v1은 `turn/start`에 `{threadId, input}`만 전송한다. `approvalPolicy`/`sandboxPolicy`/`model`/`effort` override는 보내지 않고 provider/server default에 맡긴다. experimental 필드(`environments`/`permissions`/`collaborationMode`)도 미전송이다. | 해소 완료 — `codex-app-server-adapter.test.ts` OQ-20이 outbound `turn/start` params를 정확히 `{threadId:"th_1", input:[{type:"text", text:"hi", text_elements:[]}]}`로 검증한다. settings/UI에서 model·effort·sandbox·approval override를 노출하는 것은 후속 기능이며, 그때 09 정책·05 §2.4를 함께 갱신한다. **후속(2026-07-05, ②-A)**: Claude direct 세션에 composer 모드 셀렉터(`session/set_mode` + 권위 `current_mode_update` 단일 소스)와 고위험 진입 확인 게이트를 구현했다(Codex는 여전히 turn override 미전송). 모드 전환 창의 승인 severity는 보수적으로 escalation 편향하며, 프로토콜에 전환 correlation이 없어 생기는 "같은 값 stale echo 조기 확정" 잔여 edge는 09 §8.3에 문서화·수용했다. Codex model/effort 셀렉터(②-B)와 통합 UI(②-C)는 남은 후속이다. |
| OQ-21 | **(해소됨, v1 미지원 고정)** standalone `command/exec` 채널(`command/exec/outputDelta`, base64) v1 지원 범위 | [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md) §8.1, [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) §3.3·§5.3, [`11-testing-acceptance.md`](11-testing-acceptance.md) CX-9 | **확정/구현**: v1은 thread 채널(`item/commandExecution/outputDelta`)만 지원한다. standalone `command/exec/outputDelta`는 thread/turn 없이 sandbox 실행하는 별도 base64 채널이므로 v1 mapper가 `command_output_delta`로 변환하지 않고 raw 보존+unknown counter 경계로만 처리한다. thread-less sandbox 실행 UI/terminal routing은 후속 기능 범위다. | 해소 완료 — `codex-wire-mapper.test.ts` CX-9가 standalone `command/exec/outputDelta`를 raw/unknown 경계로만 처리함을 검증하고, CX-8이 thread 채널만 `command_output_delta`로 매핑함을 검증한다. standalone 채널을 도입할 때는 05 §8.1의 base64 decode/processId routing과 08 terminal surface 정책을 새 scope로 연다. |
| OQ-22 | **(해소됨, generated schema 기준 stdout 고정)** thread 채널 `item/commandExecution/outputDelta`의 stdout/stderr 구분 | [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md) §8.1, [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) §5.2, generated `CommandExecutionOutputDeltaNotification.ts`/`CommandExecOutputDeltaNotification.ts` | **확정/구현**: thread 채널 generated payload는 `{threadId, turnId, itemId, delta}`이고 `stream` 필드가 없다. 따라서 v1 thread command output은 `command_output_delta{stream:"stdout", delta}`로 고정한다. `stream`/`deltaBase64`는 standalone `command/exec/outputDelta` payload에만 존재하며, standalone 채널은 OQ-21에 따라 v1 미지원 후속 범위다. | 해소 완료 — `codex-wire-mapper.test.ts` CX-8이 thread outputDelta→stdout 매핑을, CX-9가 standalone `command/exec/outputDelta`를 raw 보존+unknown counter로만 처리함을 검증한다. |
| OQ-23 | **(해소됨, D5)** turn cancel 소유권 — 어댑터가 `turn/interrupt` RPC 직접 전송 vs backend `agent_runtime_cancel`(`AgentRuntimeCancelTarget`)이 wire 변환 | [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md) §7.3, [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §6.3, 15 §8.1 | **확정**: frontend adapter가 `turn/interrupt`(ref-codex §3.2) 등 turn/request cancel wire를 `agentRuntimeSend`로 **직접 전송**한다. backend `agent_runtime_cancel`은 wire 변환을 하지 않는다(07 §6.3·14 §5 정본). approval cancel 응답(JSONRPCResponse)도 어댑터가 직접 send. | 해소 완료 — 05 §7.3·07 §6.3·14 §5와 일치. 03 §경계는 adapter 전송으로 기술. |
| OQ-24 | `codexBin` 경로 override(settings 노출) 및 startup preflight(`codex --version` 일치 검증) | [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md) §2.2·§11 | **부분 구현 / settings override 후속**: renderer는 `codexBin`/`command`를 넘기지 않고 backend가 `provider:"codex"`로 신뢰 절대경로를 resolve한다(OQ-36). start 경로는 spawn 전에 resolved executable로 `--version` preflight를 실행하고, probe 실패·빈 출력·상대경로는 `Err(String)`으로 거부한다. strict pin 비교는 자동 거부가 아니라 OQ-41 dependency refresh/schema diff 절차로 다룬다. settings UI binary override는 아직 미노출이다. | RS-10f/RS-10g가 version preflight가 path cache와 별개로 startup마다 실행되고 절대경로만 받는지 검증하고, RS-10h/RS-10i가 start 직전 preflight 실패·빈 출력이 spawn으로 진행되지 않는지 검증한다. settings UI에 binary 경로 override를 노출할지는 별도 제품 결정이며, 노출 시에도 backend trusted absolute path allowlist와 OQ-36 cache clear 경계를 유지해야 한다. |
| OQ-25 | tokio 도입 여부 / backend async 모델 — `std::thread`+`Arc<Mutex>` 유지 vs tokio 도입 | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §3·§7.4, [`research/codebase-backend.md`](research/codebase-backend.md) §7·§11 | **std::thread 유지**(현 backend tokio import 0건, PTY 패턴 정합) | tokio 도입은 ADR 결정 사항([`adr-001-direct-agent-runtime.md`](adr-001-direct-agent-runtime.md)). backpressure/timeout 요구가 polling으로 불충분할 때만 재검토. v1은 std::thread. |
| OQ-26 | **(해소됨, T2.3/RS-3)** `decode_utf8_stream_chunk` 공용화 방식 — `pub(super)`→`pub(crate)` 가시성 상향 vs `features/io_util.rs` 추출 | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §2, [`research/codebase-backend.md`](research/codebase-backend.md) §2.6·§11 | **확정/구현**: 최소 변경인 `pub(crate)` 가시성 상향. `agent_runtime/transport.rs`가 `features/terminal/parsing.rs`의 `decode_utf8_stream_chunk`를 직접 재사용한다. 별도 `features/io_util.rs` 추출은 하지 않는다. | 해소 완료 — transport.rs import와 RS-3 UTF-8 read-boundary 테스트가 경계 보존을 검증한다. 추출(b)에 따른 terminal 테스트 import 변경은 필요 없음. |
| OQ-27 | **(해소됨, v1 `wsl --cd`)** WSL cwd 전달 방식 — `wsl --cd <wslPath>` vs provider CLI cwd flag(codex `--cd`, claude launch param) | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §5.1, [`research/codebase-backend.md`](research/codebase-backend.md) §5.1 | **확정/구현**: v1 direct runtime은 provider별 cwd flag가 아니라 `wsl.exe -d <distro> --cd <wslWorkDir> ...`를 provider 공통 launch 경계로 사용한다. `workDir`는 `canonicalize_wsl_path`로 Windows path/relative/`~`를 거부하고 문자열 레벨 POSIX 정규화한 뒤 argv에 들어간다. | 해소 완료 — `process::build_wsl_command`가 `--cd <workDir>`를 조립하고, `agent_runtime::tests` AC-10b/RS-8 경계가 `--cd` 포함과 WSL absolute path 정규화를 검증한다. 배포 대상 WSL이 `--cd`를 지원하지 않는 예외가 발견되면 provider CLI cwd flag fallback을 새 후속 scope로 연다. |
| OQ-28 | **(해소됨, C1 보안 경계 — 06 Claude launch까지 전파)** direct runtime env 전달 방식 — secret env를 argv로 노출하지 않는다 | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §5.1, [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md) §2.2·§3.3, [`09-permissions-security.md`](09-permissions-security.md) | **확정(C1)**: env를 **non-secret / secret**으로 분리한다. **non-secret env**(비민감 플래그 등)만 launch argv(`wsl.exe … -e env KEY=VAL … <exe> <argv>`, 07 §5.1)로 넘긴다. **secret env**(API key / OAuth token / gateway header / session cookie 등)는 **절대 argv로 넘기지 않는다** — argv는 OS 관측면(`ps`, `/proc/<pid>/cmdline`, WSL process 목록)에 평문 노출되어 redaction으로 막을 수 없다. **v1 기본값**: provider 인증은 각 CLI의 WSL 측 자체 로그인/config(`claude login`, `codex auth`)에 의존하고, CLCOMX는 secret env를 런타임으로 넘기지 **않는다**. secret env를 꼭 넘겨야 하는 경우(gateway 등)의 정본 메커니즘: Rust `std::process::Command::env()`로 `wsl.exe` 프로세스 환경에 설정 + `WSLENV`(예: `WSLENV=ANTHROPIC_API_KEY/u`)로 WSL 측에 passthrough(argv 비경유). **06 Claude launch까지 전파(R1)**: 06 §2.2 `ClaudeAcpLaunchConfig.env` 및 `buildClaudeAcpLaunchParams`의 `env`는 **non-secret 전용**으로 한정한다(`AgentRuntimeStartParams.env` = launch argv `-e env KEY=VAL` 경로와 동일 경계). `buildClaudeAcpLaunchParams` 예시의 `env: cfg.env`(secret 포함 가능)는 non-secret만 싣도록 교정하고 env 필드 주석도 non-secret 전용으로 변경한다. v1 기본은 Claude도 WSL 측 자체 인증(`claude login`/config)에 의존해 secret 미전달이며, gateway 등으로 API key가 꼭 필요하면 backend가 `Command::env()`+`WSLENV`로 자식 프로세스 환경에 주입(argv 비경유)한다. 06 §3.3 auth 경로 서술도 이 정본으로 동기화한다. `AgentRuntimeStartParams.env`(15 §8.1)는 **non-secret 전용** 규약임을 06 §2.2·§3.3·07 §5.1·§11·[09](09-permissions-security.md)에 명시(15 §8.1 타입 자체는 손대지 않고 06/07/09를 인용). 평문 영속화 금지는 그대로(10 §7.3). | 해소 완료 — non-secret은 env argv, secret은 `Command::env()`+`WSLENV`(argv 금지), v1은 secret env 미전달이 기본값. C1 경계가 07 backend launch뿐 아니라 **06 Claude launch까지 통일**됐다(non-secret 전용, secret은 `Command::env()`+`WSLENV`). gateway 등 secret 전달이 실제로 필요해질 때 06 §2.2·§3.3·07 §5.1·§11과 [09](09-permissions-security.md)에서 `WSLENV` passthrough 구현·redaction을 확정. |
| OQ-29 | **(해소됨, T2.0/RS-13..15c)** child wait/kill 동시성 모델 — blocking `child.wait()`가 `Mutex` lock 유지 시 `kill()`과 deadlock | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §5.3, [`11-testing-acceptance.md`](11-testing-acceptance.md) §5.4 | **확정**: `ChildHandle`은 `spawn_child_wait` 전용 thread로 move되어 그 thread가 `child.wait()`를 소유한다. `AgentRuntime` 상태에는 kill 전용 `KillHandle { pid }`만 보관하고, shutdown은 저장된 pid로 kill을 호출하므로 wait 중 `Mutex`를 잡지 않는다. | 해소 완료 — RS-13/14/15c가 graceful exit kill 미호출, timeout 후 kill+reap 후 제거, reap timeout 시 `Err`+runtime 유지 분기를 검증한다. |
| OQ-30 | **(해소됨, D5)** agent_runtime_cancel 의미 범위 / `agent_runtime_cancel`의 backend 책임 범위 — `process` target만 처리(=shutdown), `request`/`turn`은 frontend가 wire 전송 | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §6.3, 15 §8.1, OQ-23 | **확정**: backend `agent_runtime_cancel`은 `{type:"process"}` target만 처리(=shutdown)하고, `request`/`turn` target은 **no-op**(또는 pending id 정리만)이다. turn/request cancel wire는 frontend adapter가 `agentRuntimeSend`로 직접 전송한다(OQ-23, 07 §6.3·14 §5). | 해소 완료 — OQ-23과 동일 정본. command는 15 §8.2 계약대로 유지. |
| OQ-31 | **(해소됨, v1 문자열 검증 경계)** distro allowlist 검증 여부 / WSL path 존재 검증 여부 — `list_wsl_distros` 집합 검증, `test -d` 존재 확인 | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §8·§9, [`commands/wsl.rs`] | **확정/구현**: v1은 `distro.trim().is_empty()`만 거부하고 `list_wsl_distros` 결과 집합으로 allowlist 대조하지 않는다. `workDir`는 `canonicalize_wsl_path`에서 Windows path/relative/`~`를 거부하고 문자열 레벨 POSIX 정규화(`//`, `.`, `..`, trailing slash)만 수행한다. WSL fs `test -d` 존재 검증은 start 경로에서 하지 않고, 잘못된 distro/path는 resolver/spawn 실패로 표면화한다. | 해소 완료 — `agent_runtime::tests` RS-8c가 빈 distro 거부를, RS-8 path 테스트가 Windows/relative/`~` 거부와 문자열 정규화를 검증한다. launch 전 디렉터리 존재 UX 검증(`WslShell::exec test -d`)이 필요해지면 후속으로 추가하되 v1 보안/allowlist 경계는 문자열 검증 + backend-resolved executable이다. |
| OQ-32 | **(해소됨, D10)** ACP v1 `session/update` variant 집합 개수 상충 — ref-acp §2(11종)와 ref-claude-agent-acp §2(13종)가 서로 다른 패키지 버전을 인용해 불일치 | [`ref-acp-protocol.md`](ref-acp-protocol.md) §2, [`ref-claude-agent-acp.md`](ref-claude-agent-acp.md) §2, [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md) §5·§5.1 | **확정(13종 정본)**: `user_message_chunk`, `agent_message_chunk`, `agent_thought_chunk`, `tool_call`, `tool_call_update`, `plan`, `plan_update`, `plan_removed`, `available_commands_update`, `current_mode_update`, `config_option_update`, `session_info_update`, `usage_update`. ref-acp §2 / ref-claude-agent-acp §2 / 06 §5·§5.1이 모두 이 목록·개수에 일치해야 한다. | 해소 완료 — Stage 0에서 재검증된 정본. 세 문서의 variant 목록을 위 13종으로 동기화. |
| OQ-33 | **(해소됨, H4)** Codex outbound `UserInput.text` variant의 정확한 wire 필드 — `text`만 보내는가, `text_elements`도 동반해야 schema가 통과하는가 (C3 verify-at-impl) | [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) §6.4·§6.5·§6.6, [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md) §5.3d | **확정**: `codex app-server generate-ts` 산출물(0.142.2)의 `v2/UserInput.ts`에서 `text` variant는 `text_elements: Array<TextElement>`가 필수 필드다. plain text outbound는 span이 없으므로 `{ type:"text", text, text_elements: [] }`로 보낸다. `TextElement = { byteRange: ByteRange, placeholder: string | null }`. | 해소 완료 — `makeTextUserInput(text)`는 `{ type:"text", text, text_elements: [] }`를 반환하며, Codex adapter `turn/start` 테스트가 같은 payload를 검증한다. 이전 stub-until-verified gate는 해제됐다. image capability opt-in과 resource→mention 근사 매핑은 `mapAgentContentToUserInput`의 별도 보수적 매핑으로 다룬다. |
| OQ-34 | **(해소됨, R4 allowlist 강화)** 07 §8.1 spawn allowlist가 `command` **basename만** 검사 — 절대경로 우회·임의 args·임의 env key 주입 위험 | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §8.1, [`09-permissions-security.md`](09-permissions-security.md), [`11-testing-acceptance.md`](11-testing-acceptance.md) | **확정(RD-8)**: basename 비교를 **폐기**하고 강화한다. (a) executable `command`는 renderer 입력에서 제거하고 backend가 `provider`로 resolve한 신뢰 절대경로 또는 사전 등록된 절대경로 화이트리스트만 사용한다. (b) **args 정확 검증**: Codex는 `args == ["app-server","--stdio"]` 정확히, Claude는 `args.length==2` 이고 `args[0]`이 검증된 `adapterEntryPath`(절대경로, `claude-agent-acp` `dist/index.js` 패턴), `args[1]`이 고정 `--hide-claude-auth`일 것 — 임의 `.js`/임의 바이너리/임의 argv 거부. (c) **env key allowlist**: key는 `^[A-Za-z_][A-Za-z0-9_]*$` + OQ-38에서 확정한 provider별 허용 key 집합으로 강제, 값은 non-secret(RD-14). shell metachar 검사는 방어용으로 유지. 09는 untrusted renderer 위협모델에 backend-resolved executable·args(정확 일치)·env key allowlist 재검증을 명시하고 수용 기준 추가. 11은 해당 allowlist 테스트(승인 절대경로만 통과, Codex 정확 args, Claude `adapterEntryPath`+`--hide-claude-auth` 검증, 추가 `command` 키 구조적 차단, env key allowlist) 추가. | 해소 완료 — 07 §8.1을 backend-resolved executable+정확 args+env key allowlist로 교정(basename-only 제거)하고, 09 위협모델·수용 기준 + 11 테스트를 동기화한다. |
| OQ-35 | **(해소됨, R5 무응답 폐기 금지)** 미지원 server request(`id` 있는 server→client 요청)를 무응답으로 silent-drop하면 provider가 영구 대기(deadlock) | [`04-normalized-agent-model.md`](04-normalized-agent-model.md) §5, [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md) §7.1, [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md), [`ref-acp-protocol.md`](ref-acp-protocol.md) §1, [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) §4 | **확정(RD-15)**: provider server→client **REQUEST**(JSON-RPC `id` 있음) 중 미지원 method는 **반드시 JSON-RPC error 응답**(예: `code -32601` method not found) 또는 명시적 decline/cancel 응답을 보낸다 — **무응답 silent-drop 금지**. unknown **NOTIFICATION**(`id` 없음)은 응답 불필요, raw 보존+counter로 가시화(RD-10). 04 §5에 규칙 신설. 05 §7.1 default 분기는 `routing.resolveApproval` 후 `return []`로 끝내지 말고 unknown server request에 JSON-RPC error/unsupported(또는 decline) 응답을 먼저 보낸다. 06도 동일 원칙으로 parity 응답. 11에 unknown-request 응답 테스트 추가. | 해소 완료 — REQUEST 무응답 금지(error/decline 의무), NOTIFICATION은 raw 보존+counter. 04 §5 규칙 + 05 §7.1/06 default 분기 + 11 테스트를 이 정본으로 동기화한다. |
| OQ-36 | **(해소됨, T2.0)** command/entry resolve 주체 — backend가 `provider`로 resolve하는 executable/adapter entry의 책임자·캐시 무효화·탐색 방식 | [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md) §2.2, [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md) §2.2, [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §8.1, [`11-testing-acceptance.md`](11-testing-acceptance.md) §5.3 | **확정**: owner 모듈은 `agent_runtime/resolver.rs`. `allowlist.rs`가 `resolve_trusted_executable(provider,distro)` / `resolve_trusted_adapter_entry(provider,distro)`를 호출한다. cache key는 `(provider,distro)`, 프로세스 수명 in-memory cache, TTL 없음, `clear()`로 무효화한다. Codex executable은 WSL 내부 `command -v codex`, Claude executable은 `command -v node`, Claude adapter entry는 `node_modules/@agentclientprotocol/claude-agent-acp/dist/index.js` pinned layout만 신뢰한다. | 해소 완료 — RS-10d/10e가 executable 성공경로·cache hit·clear 후 재탐색과 adapter entry pinned layout 거부/허용을 검증한다. renderer는 command를 넘기지 않고 start 시 backend가 provider로 재resolve한다. |
| OQ-37 | **(해소됨, T2.0)** serde 버전 확인 — `rename_all_fields` 사용 가능 여부 | [`15-data-contracts.md`](15-data-contracts.md) §8, [`11-testing-acceptance.md`](11-testing-acceptance.md) §5.7 | **확정**: `src-tauri/Cargo.lock` serde `1.0.228` ≥ `1.0.181`이므로 variant 필드 camelCase에는 `#[serde(rename_all_fields = "camelCase")]`를 사용한다. | 해소 완료 — RS-21..RS-25 round-trip이 `workDir`/`requestId`/`runtimeId`/`droppedMessages` 등 variant 필드가 snake_case로 새지 않음을 검증한다. |
| OQ-38 | **(해소됨, T2.0)** provider env key allowlist 집합 — Codex/Claude별 non-secret env 허용 key | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §8.1, [`09-permissions-security.md`](09-permissions-security.md), [`11-testing-acceptance.md`](11-testing-acceptance.md) §5.3 | **확정**: env 값은 non-secret 전용(RD-14). 공통 허용 key는 `RUST_LOG`, `RUST_BACKTRACE`, `NO_COLOR`; Codex 추가 허용 key는 `CODEX_DISABLE_UPDATE_CHECK`; Claude 추가 허용 key는 `CLAUDE_CONFIG_DIR`, `CLAUDE_CODE_EXECUTABLE`, `NODE_OPTIONS`. key는 `^[A-Za-z_][A-Za-z0-9_]*$` 정규식도 통과해야 하며, 집합 외 key는 거부한다. | 해소 완료 — RS-12/12b가 invalid key, 집합 외 key, provider별 key 허용/거부를 검증한다. secret env가 필요해지는 후속 범위는 argv가 아니라 `Command::env()`+`WSLENV` 경로(C1)를 사용한다. |
| OQ-39 | **(해소됨, T2.0)** backpressure 임계값 — `MAX_MESSAGE_LOG_BYTES`/`BACKPRESSURE_NOTIFY_INTERVAL`/line cap | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §7.2·§7.3, [`11-testing-acceptance.md`](11-testing-acceptance.md) §5.5, [`research/codebase-backend.md`](research/codebase-backend.md) §2.3 | **확정**: `MAX_MESSAGE_LOG_BYTES = 8 * 1024 * 1024`(8 MiB diagnostic-only bounded replay log), `MAX_LINE_BYTES = 4 * 1024 * 1024`(4 MiB; 초과 라인 → recoverable framing error), `BACKPRESSURE_NOTIFY_INTERVAL = 256`(N drop마다 telemetry event 1회). 정책은 replay log oldest drop이며 실시간 emit은 throttle/block/drop하지 않는다(OQ-50 후속). | 해소 완료 — RS-16/17이 bounded replay log trim, `droppedMessages` count, notify interval, overflow 후 후속 메시지 보존을 검증한다. |
| OQ-40 | 14와 본 표 동기화 유지 — 11 E2E 표 번호와 14 시퀀스 다이어그램의 1:1 대응 | [`11-testing-acceptance.md`](11-testing-acceptance.md) §7, [`14-sequence-and-state.md`](14-sequence-and-state.md) | **운영 항목**: 14의 시퀀스가 바뀌면 11 §7 E2E 번호와 설명을 함께 갱신한다. | 14 수정 PR에서는 `rg "14 대응|E2E-" docs/plans/agent-direct-runtime/11-testing-acceptance.md`로 표 대응을 확인하고, 누락 시 11/14를 같은 커밋에서 수정. |
| OQ-41 | **(해소됨, T0.0 preflight 완료)** 경로·외부 핀 preflight — 현재 작업 루트와 Codex/ACP/Claude adapter baseline freshness 확인 | [`00-index.md`](00-index.md) "현재 작업 경로 확인 규칙", [`HANDOFF.md`](HANDOFF.md) "시작 전 반드시 확인", [`01-source-map.md`](01-source-map.md) §4, [`12-implementation-workstreams.md`](12-implementation-workstreams.md) T0.0, [`impl-log.md`](impl-log.md) T0.0/OQ-41 | **확정/구현**: 작업 루트/git root는 `/home/xenia/work/claudemx`로 일치했다. 구현 브랜치는 `feat/agent-runtime-impl`. Codex CLI는 baseline `rust-v0.142.0` 대비 현재 `codex-cli 0.142.2`였고, `generate-ts --out` 제공 + v2 thread/turn 모델 유지로 수용했다. Claude adapter는 `@agentclientprotocol/claude-agent-acp@0.51.0` 고정 유지, transitive `@agentclientprotocol/sdk@0.29.0` package schema/dist + CLCOMX 부분 wire mirror를 ACP 구현 기준으로 삼았다. 2026-06-28 재확인 결과 public ACP latest tag는 `agent-client-protocol` `v1.1.0`(`schema/v1` 존재, `protocolVersion=1` stable), npm latest는 `@agentclientprotocol/claude-agent-acp@0.52.0`/`@agentclientprotocol/sdk@1.0.0`이다. current public `v1.1.0` schema의 `SessionUpdate`는 11종, pinned SDK 0.29.0 schema는 13종이므로 현 구현 핀을 임의 상향하지 않는다. | 해소 완료 — `impl-log.md` T0.0/OQ-41과 H4 기록, `generated/codex-app-server/README.md`, `package.json`/`package-lock.json`, `contracts/claude-acp.ts` 부분 mirror, `claude-acp-*` 테스트가 현재 구현 기준을 고정한다. 향후 Codex/ACP/Claude dependency refresh나 target 환경 재검증 때는 동일 절차를 반복하고, schema diff + fixture replay 결과로 핀 갱신 여부를 결정한다. 배포 대상 node/claude/native binary 차이는 OQ-10에 남긴다. |
| OQ-42 | **(해소됨, v1 부분 wire mirror)** sdk 타입 의존 방식 — frontend/direct adapter는 sdk 직접 import 없이 부분 wire mirror 사용 | [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md) §1.2 | **확정/구현**: frontend/direct adapter 코드는 `@agentclientprotocol/sdk`를 직접 import하지 않고 `contracts/claude-acp.ts`에 어댑터가 직접 다루는 ACP subset만 부분 wire mirror로 둔다. 공통 타입은 15 정본을 import하고, 실제 adapter process 의존성은 `@agentclientprotocol/claude-agent-acp`/transitive sdk 쪽에 남긴다. | 해소 완료 — `rg -n "^import .*@agentclientprotocol/sdk|from ['\\\"]@agentclientprotocol/sdk|\\\"@agentclientprotocol/sdk\\\"\\s*:" package.json src/lib/features/agent-runtime` 기준 직접 import/dependency 없음. drift는 `claude-acp-content/session-update/permission/initialize/adapter` 테스트와 fixture replay로 잡는다. |
| OQ-43 | **(해소됨, v1 conservative client capabilities)** client capability 1차 값 — ACP fs/terminal/terminal_output/terminal-auth/elicitation 광고 범위 | [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md) §3.2·§3.3 | **확정/구현**: `DEFAULT_CLIENT_CAPABILITIES`는 `fs.readTextFile=false`, `fs.writeTextFile=false`, `terminal=false`, `auth.terminal=false`, `auth._meta.gateway=false`, `elicitation.form=null`, `elicitation.url=null`, `_meta.terminal_output=false`, `_meta["terminal-auth"]=false`를 전송한다. 인증은 WSL 측 자체 로그인/config에 의존하고, terminal/gateway interactive auth와 form/url elicitation은 후속. | 해소 완료 — `claude-acp-initialize.test.ts` OQ-43이 기본 capability 상수와 `initialize` payload를 고정한다. `AskUserQuestion`, terminal tool output, editor file IO를 CLCOMX가 직접 대행해야 하는 요구가 생기면 capability별 UX/보안 범위를 06/08/09/11에 함께 반영한다. |
| OQ-44 | **(해소됨, v1 process-per-session)** session/close 필요성 — v1은 process shutdown만 사용하고 `session/close` 미전송 | [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md) §4.4, [`14-sequence-and-state.md`](14-sequence-and-state.md) §6.1 | **확정/구현**: Claude ACP v1은 `startSession`/`resumeSession`마다 별도 runtime process를 만드는 process-per-session 모델이다. `shutdown`은 pending approval/RPC를 정리한 뒤 `deps.shutdownRuntime`을 await하며, `session/close` wire는 보내지 않는다. | 해소 완료 — `claude-acp-adapter.test.ts` OQ-44가 shutdown 시 `shutdownRuntime(runtimeId)` 호출과 `session/close` 미전송을 검증한다. 한 process에서 여러 session을 공유하는 multiplex 구조를 도입할 때만 close/delete/fork/list capability 처리와 테스트를 추가한다. |
| OQ-45 | **(해소됨, npm dist artifact 확인)** tool content/diff 세부 매핑 및 `SettingsManager.filterEscalatingDefaultMode` 동작 재확인 | [`ref-claude-agent-acp.md`](ref-claude-agent-acp.md) §6, [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md) §7.2·§12 | **확정**: npm tarball `@agentclientprotocol/claude-agent-acp@0.51.0`에는 `src/`가 포함되지 않으므로 `dist/tools.js`/`dist/settings.js`를 확인했다. `Write`/`Edit`/structuredPatch는 `diff{path,oldText,newText}` content를 낸다. `Bash`는 `terminal_output` capability가 true일 때 terminal content + `_meta.terminal_info/output/exit`를 내고, v1처럼 false이면 console code block text fallback이다. `SettingsManager.loadAllSettings()`는 `resolveSettings({cwd})` 결과에 `filterEscalatingDefaultMode(resolved)`를 적용한다. | 해소 완료 — CLCOMX v1은 terminal output capability를 광고하지 않으므로 `_meta.terminal_output` 매핑은 비활성 경로다(OQ-43). diff/content 기본 shape는 `claude-acp-content.test.ts`와 `claude-acp-session-update.test.ts`가 고정한다. terminal output capability를 켤 때만 `_meta.terminal_info/output/exit`를 `terminal_output_delta` 또는 tool terminal content로 연결하는 후속 설계가 필요하다. |
| OQ-46 | **(해소됨, Codex reasoning completed 권위 필드 = `summary[]`)** Codex `reasoning`(thought) completed item의 권위 필드 — `summary: string[]` vs `content: string[]` 중 무엇이 권위인가 | [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) §6.3, [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md) §5.3, [`04-normalized-agent-model.md`](04-normalized-agent-model.md) §3.2.5 | **확정/구현**: generated `ThreadItem`은 reasoning item에 `summary: string[]`와 `content: string[]`를 모두 정의하지만, `codex app-server --stdio` read-only snapshot 실측(2026-06-30, `thread/list` 48개 thread → `thread/read{includeTurns:true}`)에서 reasoning item 2,026개가 모두 `summaryOnly`였고 `contentOnly`/`both`/`neither`는 0개였다(`summarySegments=6,685`, `contentSegments=0`). v1 구현은 completed reasoning item을 thought 채널 권위 이벤트로 emit하고, 권위 텍스트는 `summary[]` join으로 만든다. `summary[]`가 비어 있는 schema-drift/legacy 입력만 `content[]` fallback으로 보존한다. | 해소 완료 — `codex-wire-mapper.test.ts` CX-5c가 summary authority와 content fallback을 고정한다. 향후 Codex schema/API가 `content[]`를 채우는 형태로 바뀌면 protocol drift 항목으로 재검토한다. |
| OQ-47 | approval inline/modal 분류 신호 매핑 — 어떤 provider 신호를 `severity:"escalation"`(modal)로 올릴지 | 15 §5 `ApprovalRequest.severity`, [`09-permissions-security.md`](09-permissions-security.md) §8.3, [`08-ui-composition.md`](08-ui-composition.md) §4.4, [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md)·[`06-claude-acp-adapter.md`](06-claude-acp-adapter.md) approval 매핑 | **부분 구현 / 추가 신호 후속**: v1 approval 기본 severity는 `normal`(inline)이되, [09](09-permissions-security.md) §8.3 고위험 집합은 v1부터 `severity:"escalation"`(modal)이다. 구현된 신호는 Claude `current_mode_update` 추적 기반 `bypassPermissions`, ExitPlanMode `bypassPermissions` optionId, Codex `item/permissions/requestApproval`, command `proposedExecpolicyAmendment`/`proposedNetworkPolicyAmendments`/`networkApprovalContext`, raw/future `additionalPermissions` 중 network enable/fs write overlay, raw/future `commandActions`의 `write`/`edit`/`delete`/`move`/`execute`/`fetch`류 부수효과 action type, raw/future `danger-full-access`/`Agent (Full Access)` mode field, fileChange `grantRoot`다. 현재 generated `commandActions`의 `read`/`listFiles`/`search`/`unknown`과 read-only fs overlay는 normal로 유지한다. | 현재 신호는 `claude-acp-permission.test.ts` CL-23/current mode와 `codex-wire-mapper.test.ts` CX-11b/OQ-47 commandActions로 고정한다. 남은 범위는 protected path 쓰기, 현재 generated command approval params에 없는 `availableDecisions` 등 wire 의미가 아직 불명확한 추가 escalation 신호 확대다. |
| OQ-48 | **(해소됨, T1.4)** 멀티 윈도우 registry 윈도우 소유권/수명 모델 — 12 T1.4 module-level registry의 window 바인딩·정리·dispatch 경계 | [`12-implementation-workstreams.md`](12-implementation-workstreams.md) T1.4, 위험 §1.11 | **확정/구현**: registry는 sessionHandle별 단일 소유 window label과 optional `runtimeId` 바인딩을 가진다. window-close는 자기 소유 엔트리만 정리하고 타 윈도우 엔트리/runtimeId는 보존한다. dispatch는 명시 sessionHandle로만 수행되어 미등록/타 윈도우 세션으로는 전달되지 않는다. pending approval 권위 key는 `(sessionHandle, requestId)`라 전역 단독 `requestId` 충돌이 없다. | 해소 완료 — `agent-event-router.test.ts`가 window-close 격리, cross-window dispatch 차단, 동일 JSON-RPC id를 쓰는 두 runtime의 approval 독립 라우팅을 검증하고, `pending-approval-table.test.ts`가 `(sessionHandle, requestId)` 복합 키 충돌 회귀를 검증한다. |
| OQ-49 | **(해소됨, 07/RS-5b/AC-4b)** framing 붕괴 latch 노출 범위 — 07 §4.4 latched-failed 플래그를 reader-local `bool`로 둘지, `AgentRuntime` 공유 상태로 노출해 `AgentRuntimeSnapshot.status`(15 §8.1)에 반영할지 | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §4.2·§4.4 | **결정**: v1은 reader-local `FramingState`를 사용한다. fatal framing break 후 stdout line은 drop되고 추가 framing error/message emit은 suppress된다. frontend는 1회 emit된 `recoverable:false`로만 인지하며, `AgentRuntimeSnapshot`에는 framing-failed 상태를 노출하지 않는다. | 해소 완료 — `ac4b_fatal_framing_latches_and_suppresses_followup_stdout`가 `RuntimeEventEmitter` 캡처로 fatal 1회·후속 stdout emit 0건을 검증한다. snapshot 노출은 진단/late-attach 요구가 생기면 후속으로 07 §3·15 §8.1에 추가한다. |
| OQ-50 | emit-throttle / coalesce / buffer-drop 후속 — v1 diagnostic-only bounded replay log를 넘어 실제 emit 압력 완화(throttle/coalesce/drop policy, emit buffer cap)를 도입할지 | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §7.2, 위험 §1.8 | **후속(v1 미도입)**: v1은 bounded replay log + telemetry(`droppedMessages`)만이며 emit 자체는 throttle/coalesce/drop하지 않는다(frontend는 실시간 stream 그대로 수신). 대용량 delta 폭주로 UI 렌더 부하가 실측되면 emit cap/coalesce/buffer drop policy를 도입. | 대용량 출력 fixture로 UI 렌더 부하를 실측한 뒤 throttle/coalesce/drop 수치·정책을 정하고 07 §7.2·11 §에 반영. v1은 diagnostic-only bounded log를 유지. |
| OQ-51 | **(해소됨, v1 in-memory audit / 영속 로그 후속)** approval audit trail 저장 위치·보존기간·포맷 | [`09-permissions-security.md`](09-permissions-security.md) §3.4, [`12-implementation-workstreams.md`](12-implementation-workstreams.md) audit task, [`11-testing-acceptance.md`](11-testing-acceptance.md) audit 수용 | **확정/구현**: v1 기본 audit trail은 `AgentRuntimeStore` 수명 안의 in-memory `ApprovalAuditTrail`이다. 세션 teardown/store `dispose()` 때 clear되며 디스크·workspace/history에는 영속하지 않는다. 레코드 포맷은 `sessionHandle`/`provider`/`requestId`/`optionId?`/`optionKind?`/`toolCallId?`/`outcome`/`decidedAt`/`decidedBy{user\|auto\|cleanup}` 메타만 포함하고, 명령 전문/credential/파일 내용/raw label은 타입 경계에 넣지 않는다. opt-in redacted 영속 로그는 v1 기본 구현에 포함하지 않고 후속 enhancement에서 저장소(파일 vs OS store)·retention·레코드 포맷을 별도 결정한다. | 해소 완료 — `agent-runtime-store.svelte.test.ts` NM-20b/NM-20c가 user/auto/cleanup 결정 1건당 audit entry 1건, cleanup 다중 pending 1:1, shutdown→exit 멱등 1건, 명령 전문/credential/파일 내용/raw label 비포함을 검증한다. 영속 audit 요구가 생기면 09 §3.4 최소 필드를 확장하지 말고 redacted persistence 전용 task/test로 추가한다. |
| OQ-52 | **(부분 해소, v1 기본 cap 구현 / 실측 튜닝 후속)** long-session transcript 윈도우/cap 수치 | 위험 §1.12, [`04-normalized-agent-model.md`](04-normalized-agent-model.md) §3.7, [`08-ui-composition.md`](08-ui-composition.md) §5, [`13-risks-open-questions.md`](13-risks-open-questions.md) §1.8, [`12-implementation-workstreams.md`](12-implementation-workstreams.md) T1.1·T1.3·T1.4 | **v1 기본값 확정/구현**: `HOT_WINDOW_SEALED_TURNS=50`, `HOT_WINDOW_BYTES=8MiB`, `TOMBSTONE_LRU=200`, `SEAL_QUIESCENCE_GRACE_MS=250`. reducer는 sealed turn count cap과 byte cap을 모두 적용하고, terminal stdout/stderr·tool raw·diff/image data URI 같은 item body 근사 byte를 hot-window 계산에 포함한다. tombstone LRU cap은 turn 메타도 pruning해 `turnsById`/`itemVersions`가 세션 길이에 비례해 자라지 않게 한다. `agent-runtime-store.svelte.test.ts`는 기본 cap으로 1,000 turn synthetic fixture를 구동해 반응형 표면·heap body·turn meta·tombstone LRU가 cap에 수렴하고, tombstone late drop 및 sealed-retained late patch/reseal 분기가 유지되는지 검증한다. **잔여 후속**: 실제 대용량 세션에서 이 운영값을 조정할지, tombstone TTL을 추가할지, heavy item(diff/image/output)별 별도 ring/summary 표현 한도가 필요한지는 성능 실측 뒤 결정한다. | 부분 해소 완료 — `agent-event-reducer.test.ts` NM-31/31c/31d/31e/31f/31g/31h/32/32b/33/33b/33c/33d/33e/33f/34/34c/34d/35와 `agent-runtime-store.svelte.test.ts` NM-31b/OQ-52가 turn-count cap, byte cap, terminal stderr·tool raw·image data URI·file diff byte 포함, tombstone LRU, reactive surface bounded, 1,000-turn default-cap convergence, late-event 분기를 검증한다. 운영 튜닝은 출시/성능 pass에서 실제 대용량 세션으로 별도 측정해 조정한다. |
| OQ-53 | seal 후 same-turn 보조 notification 도착 여부 — 종료신호(Codex `turn/completed`·ACP `stopReason`) 뒤 같은 turn의 `tokenUsage`/`plan`/`diff`/늦은 `tool_call_update`가 실제 wire에서 오는가 | 위험 §1.12, [`04-normalized-agent-model.md`](04-normalized-agent-model.md) §3.7, [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md), [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md), [`11-testing-acceptance.md`](11-testing-acceptance.md) | **부분 구현 / Codex 단순·tool-bearing·approval request·fileChange diff turn 각 1회 실측 / ACP synthetic late-candidate E2E 검출 / live ACP·plan 미정**: reducer/store 경계는 보수 기본값을 구현했다. sealed-retained turn에 늦은 same-turn event가 오면 unseal→patch→reseal+telemetry로 흡수하고, evicted-tombstone 구간은 apply 금지 + `droppedLateEventCount`만 증가한다. opt-in raw debug log는 inbound entry에 reader-local `seq`를 기록한다. `analyzeRawProtocolDebugLog`는 해당 JSONL에서 Codex `turn/completed` 실제 envelope(`params.threadId` + `params.turn.id`) 및 ACP `session/prompt` stopReason response marker를 `terminalMarkers[]`로 노출하고, 그 뒤 같은 routing key의 notification 후보를 `lateEvents[]`로 판정한다. **2026-06-30 Codex app-server stdio probe(수동 앱 launch 없음)** 에서는 tool-free simple turn 1회에서 `thread/tokenUsage/updated`가 inbound `seq=25`, `thread/status/changed`가 `seq=27`, `turn/completed`가 마지막 inbound `seq=28`로 도착했고, `turn/completed` 뒤 1.5초 grace 동안 `lateEvents=[]`였다. 같은 날 tool-bearing shell command turn 1회에서는 commandExecution `item/started` seq 19, `item/completed` seq 20, `thread/tokenUsage/updated` seq 21/30, `thread/status/changed` seq 32, `turn/completed` seq 33, completion 뒤 2초 grace 동안 `lateEvents=[]`였다. **2026-07-01 Codex approval request probe(수동 앱 launch 없음)** 에서는 read-only sandbox command turn 1회에서 `item/commandExecution/requestApproval` seq 20, client `{decision:"accept"}` 응답, `serverRequest/resolved` seq 21, `item/completed` seq 23, `thread/tokenUsage/updated` seq 24/34, `turn/completed` seq 37, completion 뒤 2.5초 grace 동안 `lateEvents=[]`였다. **2026-07-01 Codex fileChange diff probe(수동 앱 launch 없음)** 에서는 workspace-write file edit turn 1회에서 fileChange `item/started` seq 66, fileChange `item/completed` seq 67, `turn/diff/updated` seq 68/71/82, `thread/tokenUsage/updated` seq 69/80, `turn/completed` seq 84, completion 뒤 2.5초 grace 동안 `lateEvents=[]`였다. 같은 prompt에서 plan을 요청했지만 `turn/plan/updated`/plan item은 발생하지 않았다. **2026-07-01 Windows agent-runtime E2E(수동 앱 launch 없음)** 는 test-mode Claude ACP synthetic raw log에서 `session/prompt` out(sessionId `sess-1`) → `session/update:agent_message_chunk` seq 3 → stopReason response seq 4 → delayed `session/update:plan_update` seq 5를 analyzer가 `lateEvents[]`로 검출하는 경계를 고정했다. 이는 검출 경계 증거이며 live Claude wire evidence가 아니다. | `agent-event-reducer.test.ts` NM-33/NM-33b가 reducer/store event fixture로 sealed-retained late patch와 추가 completed 없이 reseal되는 경로를 고정한다. `agent_runtime::tests::e2e10_raw_protocol_debug_log_is_opt_in_and_redacted`는 inbound raw debug log `seq` 기록을 고정한다. `agent_runtime::tests::e2e10_outbound_protocol_debug_log_is_opt_in_and_redacted`와 `rs19_mock_jsonrpc_responses_are_valid_and_provider_specific`는 outbound raw debug ordering과 mock routing sessionId를 고정한다. `debug-log-analyzer.test.ts`는 Codex/ACP terminal marker와 late 후보 판정 경계를 고정한다. `e2e/agent-runtime/agent-runtime.test.ts` E2E-10은 opt-in raw debug log를 analyzer로 읽어 일반 prompt와 approval mock turn의 Codex terminal marker가 존재하고 `lateEvents=[]`임을 확인하며, synthetic Claude ACP late candidate가 `lateEvents[]`로 검출됨을 확인한다. Codex rollout JSONL은 raw app-server notification envelope이 아니라 app history/event stream이므로 OQ-53 wire evidence로 쓰지 않는다. 남은 결정은 plan 포함 Codex live turn, Claude ACP live `session/prompt` stopReason 뒤 `session/update` 후보를 추가 캡처해, 실제 same-turn 보조 notification 도착 여부와 종류에 따라 seal grace 길이·adapter fixture 필요 여부를 조정하는 것이다. |
| OQ-54 | **(해소됨, full snapshot / partial notice 방어)** Codex `thread/read(includeTurns)` 복원 범위 — gap-only 복원인가 전체 snapshot인가(격리 replay 범위·비용) | 위험 §1.12, [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md), [`ref-acp-protocol.md`](ref-acp-protocol.md) §3.4, [`10-persistence-migration.md`](10-persistence-migration.md) §4.7, [`12-implementation-workstreams.md`](12-implementation-workstreams.md) T5.6 | **확정/구현**: `codex-cli 0.142.2` app-server 실측(2026-06-29)에서 `thread/list{limit:5}`의 listed thread는 `turns:[]`, 같은 thread에 대한 `thread/read{includeTurns:false}`도 `turns.length=0`, `thread/read{includeTurns:true}`는 전체 thread snapshot을 반환했다(현재 thread 기준 `turns.length=32`, 첫 turn `items.length=33`, `itemsView:"full"`). 따라서 현재 Codex API에서 tombstone gap-only 조회는 지원하지 않는 것으로 본다. Codex adapter `resumeSession({replay:true})`는 `thread/read{threadId, includeTurns:true}`를 보내고, returned `thread.turns[].items[]`를 completed item replay로 재생한다. UI scrollback 기본 경로는 `createPortReplayLoader`로 provider port-backed scratch `resumeSession({replay:true})`를 열며, `ReplayLoader`/`loadReplayTranscript`가 read-only scratch transcript와 live 미병합 경계를 유지한다. full snapshot 비용 방어로 provider port 수집과 scratch reduce 모두 `DEFAULT_REPLAY_EVENT_LIMIT`(기본 1000)까지만 반영하고, 상한 초과 시 `ReplayPanel`이 partial snapshot notice를 표시한다. no-op loader는 provider id/capability가 없을 때의 fallback이다. | 해소 완료 — 실측 command는 권한 상승 상태에서 `codex app-server --stdio`에 `initialize`→`thread/list`→`thread/read(includeTurns:false/true)`를 보낸 로컬 read-only probe다. `codex-app-server-adapter.test.ts` CX-2가 `includeTurns:true` outbound와 replay event 생성을 고정하고, `runtime-replay.test.ts`가 canLoad gating + isolated scratch reduce + provider port-backed scratch replay + no-op fallback + full snapshot event 상한을 검증한다. `ReplayPanel.test.ts`는 상한 초과 시 partial snapshot notice가 표시되는지 검증하고, `AgentTranscriptSurface.test.ts`는 주입 loader가 없을 때도 기본 provider replay loader가 호출되는지 고정한다. 더 정밀한 gap 조회가 Codex API에 추가되면 별도 protocol drift 항목으로 재검토한다. |
| OQ-55 | **(해소됨)** transcript mono 폰트 소스 — 코드/명령 출력 mono를 기존 `settings.terminal.fontFamily` 재사용으로 구동할지 agent-runtime 전용 mono 설정을 신설할지 | [`08-ui-composition.md`](08-ui-composition.md)(설정 소비), 15 §(`UI_CSS_VARS.fontMonoStack`), [`research/codebase-frontend.md`](research/codebase-frontend.md)(Terminal.svelte `terminalFontFamily`) | **확정/구현**: transcript surface는 settings store를 직접 import하지 않고 정본 `--ui-*` CSS 토큰만 소비한다. 코드/명령 출력 mono = `UI_CSS_VARS.fontMonoStack = "--ui-font-mono-stack"`이며 `getUiPreferenceTokenStyle`가 `settings.terminal.fontFamily`/`fontFamilyFallback`로 구동한다. **agent-runtime 전용 mono 설정/섹션은 신설하지 않는다**(기존 TerminalSettings/InterfaceSettings 재사용). 하드코딩 rem·미정의 `--color-*`/`--font-mono` 금지. | 해소 완료 — `theme-bridge.test.ts`가 `--ui-font-mono-stack` 토큰이 terminal font 설정에서 만들어지고 interface font와 섞이지 않음을 검증한다. 전용 mono 설정 요구가 실측되면 후속에서 별도 토큰/섹션을 재검토한다. |
| OQ-56 | composer 명령/완성 트리거 — `$` v1 의미·소스 미정(변수/skill), Codex provider-backed slash 부재 시 client-side 정적만으로 충분한가 + `@`-mention 소스(skills/list·fuzzyFileSearch) 범위 | [`08-ui-composition.md`](08-ui-composition.md) §6.6, [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md)/[`06-claude-acp-adapter.md`](06-claude-acp-adapter.md), 15 §(`available_commands_updated`/`AgentCommand`/`searchResources`) | **부분 구현 / 잔여 있음**: `/` 명령 팔레트는 구현됨(provider `availableCommands` + 로컬 `/resume`). ACP `available_commands_update`→`available_commands_updated{ref;commands:AgentCommand[]}` emit과 store `availableCommands` 전체 교체, composer prefix filter/Enter·Tab 선택이 연결돼 있다. Codex는 provider-backed slash 소스가 없어 client-side 정적 `/resume`만 노출한다(05 명시). 단, 로컬 `/resume`은 v1에서 draft를 `/resume `로 채우는 **평문 prompt command**이고 `AgentRuntimePort.resumeSession` 직접 호출이 아니다. ACP `promptCapabilities.image`/`embeddedContext`는 `SessionStartResult.composerCapabilities`를 거쳐 store `capabilities`까지 전달되고, adapter `sendPrompt`가 unsupported `image`와 text 포함 embedded `resource` content를 `session/prompt` 직전 gate한다. text 없는 resource는 baseline `resource_link`로 보존한다. 모든 content가 gate에서 제거되면 빈 prompt를 보내지 않고 recoverable error로 낮춘다. Codex도 `mapAgentContentToUserInput(...)` 결과가 `[]`이면 빈 `turn/start{input:[]}`를 보내지 않고 recoverable error로 낮춘다(CX-4c). `@` file/resource mention은 workspace file + Codex fuzzy file + Codex skill 1차 범위가 구현됐다: `AgentRuntimePort.searchResources`를 통해 Codex `fuzzyFileSearch{query, roots:[workDir], cancellationToken:null}` 결과를 file URI 후보로 정규화하고, `skills/list{cwds:[workDir]}` enabled skill 결과는 `resourceKind:"skill"`/`text:<skill name>` 후보로 정규화해 outbound `UserInput{type:"skill", name, path}`로 보존한다. provider 결과가 없거나 실패하면 `AgentTranscriptSurface`가 기존 `searchSessionFiles(sessionId, workDir, query, 8)` fallback으로 후보를 공급한다. composer는 draft 끝의 `@query` token을 선택 후보의 `@relative/path ` 또는 `@skillName ` token으로 교체하며, 전송 payload에 원문 text와 `resource{uri:"file://..."}` content를 함께 넣는다. resource action button은 `resourceSearch` source가 있을 때만 표시되고 입력 가능 상태에서 클릭 시 draft 끝에 `@` token을 열어 같은 resource palette 흐름으로 진입하며, 빈 query도 controller/source에 전달해 provider나 workspace source가 기본 후보를 제공할 수 있게 한다. 선택된 resource는 해당 token이 공백 경계 안에 그대로 남은 경우에만 전송되며, token이 다른 단어로 편집되면 stale resource를 붙이지 않는다. WSL path의 공백·`#`·`?`는 file URI path segment percent-encoding으로 보존한다. image attach control은 `ComposerCapabilities.image`가 true일 때만 노출되고, 파일 선택·clipboard paste·drag/drop으로 들어온 `image/*` file을 data URI `AgentContent.image`로 전송하며 image-only prompt도 허용한다. draft가 비어 있지 않으면 file picker는 draft 끝에, paste/drop은 textarea cursor/selection 위치에 locale 기반 image reference token(`[Image #N]`/`[이미지 #N]`)을 삽입하고, attachment chip은 같은 token을 표시한다. attachment 제거 시 대응 token도 draft에서 제거하고 남은 attachment/token은 현재 prompt 순서로 재번호 매김한다. prompt 전송 후 다음 prompt의 image reference는 다시 #1부터 시작한다. `$`는 v1 보류. | `/` 경로는 adapter/store/composer 테스트로 고정한다. capability 전달과 ACP prompt content gate는 `claude-acp-adapter.test.ts`와 `agent-runtime-controller.test.ts`가 고정하고, Codex empty prompt guard는 `codex-app-server-adapter.test.ts` CX-4c가 고정한다. workspace/Codex fuzzy file/skills `@` 경로, resource action button, stale token 제외, file URI encoding, image capability gate + file 선택/paste/drop image-only prompt + reference token/renumbering은 `AgentComposer.test.ts`, `AgentTranscriptSurface.test.ts`, `agent-runtime-controller.test.ts`, `codex-app-server-adapter.test.ts`, `codex-wire-mapper.test.ts`가 고정한다. 남은 범위는 (1) `@` source를 ACP resource source 같은 richer provider source로 확장할지, (2) provider-backed richer image resource source가 필요한지, (3) `$` 변수/skill trigger를 v1 이후 도입할지, (4) 명시적 resume picker/command가 필요하면 provider id 보존·scrub 정책과 충돌하지 않게 설계할지다. |
| OQ-56a | ACP resource source provider-backed 검색 경계 — 현재 핀에서 구현 가능한 ACP resource search/list RPC가 있는가 | [`08-ui-composition.md`](08-ui-composition.md) §6.3/§6.6, [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md), `@agentclientprotocol/sdk@0.29.0`, `@agentclientprotocol/claude-agent-acp@0.51.0` | **v1 경계 확정 / 코드 변경 불필요**: 현재 설치 핀의 ACP schema/dist에는 prompt content `resource_link`/embedded `resource`와 `PromptCapabilities.embeddedContext`만 있고 provider-backed resource search/list request가 없다. Claude ACP adapter는 `AgentRuntimePort.searchResources`를 구현하되 `[]`를 반환하며 outbound ACP request를 만들지 않는다. `AgentTranscriptSurface`는 provider 결과가 없을 때 workspace file fallback으로 후보를 공급한다. | `claude-acp-adapter.test.ts` OQ-56이 빈 결과와 outbound 0건을 고정하고, `AgentTranscriptSurface.test.ts` OQ-56이 ACP 빈 결과 시 workspace fallback을 고정한다. 향후 ACP protocol/package가 resource search/list RPC를 추가하거나 제품이 별도 client-side provider source를 요구하면 OQ-56 본문 residual로 다시 설계한다. |
| OQ-57 | **(해소됨, v1)** ACP 로컬 `user_message` echo의 합성 messageId 충돌·비텍스트 정확도 — 합성 id `<sessionId>:t<n>:u`가 향후 ACP 라이브 `user_message_chunk` 도입 시 wire id와 충돌/중복하지 않는지 + image/resource echo가 원본 `AgentContent[]`로 정확한지 | [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md) §3.6, [`04-normalized-agent-model.md`](04-normalized-agent-model.md) §3.1, 15 §(`user_message`/`AgentContent`) | **확정/구현**: ACP는 라이브 prompt를 wire echo하지 않으므로 어댑터 `sendPrompt`가 로컬 optimistic `user_message` AgentEvent를 emit한다. messageId는 active turn id에 `:u`를 붙인 `<sessionId>:t<n>:u`, content는 ACP wire 변환본이 아니라 원본 `input.content: AgentContent[]`, emit 순서는 running 전이다. turnSeq 증가로 다음 turn은 `<sessionId>:t<n+1>:u`라 v1 내부 합성 id끼리 충돌하지 않는다. **Codex는 echo 금지**(item userMessage wire echo와 이중 렌더 방지). | 해소 완료 — `claude-acp-adapter.test.ts`가 텍스트 echo/running 전 순서와 image+resource 원본 content 보존, turn별 messageId 유일성을 검증한다. 향후 ACP가 라이브 `user_message_chunk`를 도입하면 wire id와 합성 id의 네임스페이스 충돌·중복 렌더 여부를 별도 protocol drift 항목으로 재검토한다. |
| OQ-58 | auto-follow × 가상화 상호작용 — 가상화(§7.2)가 바닥 streaming item을 unmount하면 `scrollTop=scrollHeight` 추종이 잘못 점프 | [`08-ui-composition.md`](08-ui-composition.md) §3·§7.1·§7.2, 위험 §1.13 | **부분 구현 / 가상화 후속**: auto-follow DOM 부수효과는 구현됨 — `.transcript-region` 기준으로 새 item/delta 도착 시 `scrollTop=scrollHeight`를 `tick()`로 디바운스하고, `onscroll`로 추종 on/off를 토글하며, approval은 별도 `scrollIntoView` 경로를 탄다. 단 v1 `MessageList`는 실제 scroll-window DOM 가상화가 아니라 residency eviction으로 bounded된 `visibleItemIds` 전체를 mount한다. 따라서 `streamingItemId`+바닥 N개 always-mount 게이트는 **DOM 가상화 도입 시 필요한 후속 불변식**이다. | auto-follow/approval scroll은 `AgentTranscriptSurface.test.ts`로 고정한다. DOM 가상화를 도입하는 시점에 `streamingItemId`+바닥 N개 always-mount 게이트, approval anchor mount 보장, scrollHeight 점프 회귀를 11 § fixture로 추가하고 OQ-58을 재검토한다. |
| OQ-59 | **(해소됨, v1 internal raw bridge / public diagnostic 미노출)** realtime raw protocol event redaction 경계 — adapter routing용 `agent-runtime-message` raw event를 무손실로 유지할지, redacted public event와 raw private adapter channel로 분리할지 | [`09-permissions-security.md`](09-permissions-security.md) §5 구현 점검, [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) §7.2·§11, 15 §8.3 `AgentRuntimeEvent` | **확정/구현**: v1은 `agent-runtime-message`를 provider adapter routing/normalization 전용 in-process raw bridge로 유지한다(M-4). backend는 `JsonRpcMessage`를 redaction 없이 무손실 emit하고, Codex/Claude adapter/factory deps만 이 raw channel을 provider wire 입력으로 사용한다. 화면·저장·diagnostic 파일에는 redacted projection만 노출한다. public helper인 `createAgentTransportController`는 stderr/exit/error/backpressure 4개 diagnostic channel만 구독하고 raw `message` handler를 노출하지 않는다. `AGENT_RUNTIME_EVENT_NAMES` tuple은 adapter/factory deps의 channel 정본이며, production `runtime-port-factory` deps가 Tauri event channel과 payload `type`을 대조해 잘못된 channel에 실린 raw payload를 adapter로 넘기지 않는다. | 해소 완료 — `transport.test.ts`가 public diagnostic controller의 raw `agent-runtime-message` 미구독을 검증하고, `runtime-port-factory.test.ts`가 Codex/Claude production deps의 channel/type guard를 검증한다. 더 엄격한 raw private channel + redacted public event 분리는 v1 요구가 아니며, 제품/보안에서 "모든 realtime event 무평문"을 별도 요구할 때 후속 hardening으로 07 event schema, 15 §8.3 타입, adapter transport, 11 E2E-10/09 redaction 테스트를 함께 갱신한다. |
| OQ-60 | post-start direct runtime fatal/exit recovery UX와 typed failure-code taxonomy — 기동 완료 후 `agent-runtime-exit`/`agent-runtime-error(recoverable:false)`를 PTY fallback 선택지로 승격할지, 실패 원인을 stable code enum으로 계약화할지 | [`14-sequence-and-state.md`](14-sequence-and-state.md) §5·§8, [`11-testing-acceptance.md`](11-testing-acceptance.md) CX-18/19·CL-25·RS-5b, 15 §8.3 `AgentRuntimeEvent` | **부분 구현 / UX·provider taxonomy 후속**: v1 fallback panel trigger는 fresh direct start 실패(`startSession` reject)와 provider restore 실패 후 fresh start까지 실패한 경우로 한정한다. 기동 완료 후 process exit 또는 `recoverable:false` runtime error는 현재 `process_exited`/`error` notice와 `failed`/`exited` status 경로로 남기며, 자동 PTY fallback이나 fallback panel 승격은 하지 않는다. transport/backend가 안정적으로 분류 가능한 framing error에는 optional `AgentRuntimeErrorCode` seed를 추가했다(`framing_invalid_json`/`framing_line_too_large`/`framing_broken`). 1회 invalid/cap 초과는 원인별 code를 보존하고, 5회 연속 invalid/cap 초과 또는 EOF-like fatal framing으로 latch에 들어가는 순간은 `framing_broken`으로 승격한다. Windows `agent-runtime` E2E는 test-mode post-start `agent-runtime-error{recoverable:false, code:"framing_broken"}`가 transcript notice로 남고 fallback panel로 승격되지 않는 앱 경계 하한을 고정한다. 전체 provider별 failure taxonomy와 post-start recovery UX는 아직 후속이다. | post-start recovery UX를 추가하려면 04 상태 전이, 14 §5/§8 sequence, 15 event/error 타입, `AgentTranscriptSurface` UI 액션, adapter/backend error code 매핑을 함께 갱신한다. 그 전까지 테스트는 start 실패 fallback(E2E-6/FE fallback)과 post-start notice/cleanup(CX-19/CL-25/RS-5b)을 분리해서 유지한다. `AgentTranscriptSurface.test.ts` OQ-60은 post-start `recoverable:false`와 abnormal `process_exited`가 fallback panel/PTY callback으로 승격되지 않는 v1 하한을 고정한다. `e2e/agent-runtime/agent-runtime.test.ts`는 같은 no-fallback 하한을 Windows app-boundary에서 확인한다. `transport.test.ts`/`codex-app-server-adapter.test.ts`는 runtime error `code` 보존을, Rust `ac4b_fatal_framing_latches_and_suppresses_followup_stdout`와 `oq60_repeated_invalid_lines_escalate_to_framing_broken_code_and_latch`는 fatal framing의 `framing_broken` emit과 latch를 고정한다. |
| OQ-61 | tool location click의 실제 editor/file-open wiring — direct runtime host에서 `ToolCallUpdate.locations[]`를 기존 internal/external editor 흐름으로 어떻게 연결할지 | [`08-ui-composition.md`](08-ui-composition.md) §4.3, [`11-testing-acceptance.md`](11-testing-acceptance.md) FE-7a, `Terminal.svelte` editor facade / `terminal-editor-integration-controller.ts` 경계 | **부분 구현 / 실제 설치 editor focus 검증 잔여**: `ToolCallCard`는 callback이 주입되면 location row를 접근 가능한 버튼으로 렌더하고 stable `agentToolLocationTestId(itemId,index)`와 표시 redaction/raw `FileLocation` 전달 분리를 제공한다. `MessageList`와 `AgentTranscriptSurface`는 `onOpenLocation(FileLocation)` callback을 상위로 전달한다. `SessionShell` direct host는 기존 editor facade와 `TerminalEmbeddedEditorSurface`를 직접 소유하고, terminal file-link action 정책(`fileOpenTarget`/`fileOpenMode`/`defaultEditorId`)을 direct location에도 적용한다. internal target은 WSL 절대 path와 session `workDir` 기준 상대 path를 editor path로 넘기고 line/column을 open tab state와 persisted session editor state에 보존한다. direct embedded editor foreground 오류는 direct notice surface로 표시한다. `InternalEditor` shell은 E2E 관찰용 `data-active-path`/`data-active-line`/`data-active-column`을 노출한다. `CLCOMX_TEST_MODE`의 `read_session_file`은 세션 root 허용 검사를 통과한 `src/lib/example.ts` fixture를 제공해 internal E2E 입력을 안정화한다. external target은 `resolve_terminal_path`로 `ResolvedTerminalPath.windowsPath`를 보강한 뒤 configured default editor 또는 picker 선택 editor로 연다. test-mode resolver는 같은 fixture를 실제 FS 없이 `ResolvedTerminalPath`로 해석하고, 기본 test-mode `open_in_editor`는 stateDir `editor-open-events.jsonl`에 editor/path/line/column payload를 기록한다. 추가로 `CLCOMX_TEST_MODE_EDITOR_REAL_LAUNCH=1`을 켜면 payload 기록 후 override editor process도 실제 spawn하며, E2E fake `cursor.cmd`가 `--goto <path>:12:4` argv를 받는지 확인한다. Codex mapper는 generated baseline 밖 optional `commandExecution.locations[]`도 valid `FileLocation`으로 보존하고, test-mode Codex mock은 `location` prompt에서 locations 포함 commandExecution을 emit한다. | 현재 하한은 `ToolCallCard.test.ts`, `MessageList.test.ts`, `AgentTranscriptSurface.test.ts`, `SessionShell.test.ts` OQ-61이 callback 경계, direct internal editor open, external default editor, external picker flow, foreground error notice를 검증한다. `InternalEditor.test.ts`는 active path/line/column E2E 관찰 지점을 고정하고, `codex-wire-mapper.test.ts` CX-10b와 Rust `rs19_mock_jsonrpc_responses_are_valid_and_provider_specific`, `read_session_file_returns_test_mode_location_fixture`, `test_mode_resolves_location_fixture_without_real_file`, `test_mode_open_in_editor_records_launch_payload`, `test_mode_real_editor_launch_spawns_override_with_line_args`는 mapper/mock/read/resolve/open payload 및 real-launch test-mode spawn 기반을 검증한다. `e2e/agent-runtime/agent-runtime.test.ts`의 E2E-11 internal target spec, external default-editor/picker command-payload spec, fake external editor process real-launch spec은 최신 Windows `agent-runtime` E2E(`npm run test:e2e:wsl -- --project agent-runtime`, 1 file / 18 tests)에서 통과했다. 남은 범위는 fake process argv 확인을 넘어 실제 설치 editor(Cursor/VS Code 등) process의 focus/line reveal을 검증하는 것이다. |

> 레지스트리 운용: 항목이 해소되면 출처 문서에서 "verified"로 승격하고 본 표에서 제거하거나 "(해소됨)"으로 표기한다(ref-codex §10·ref-acp §14의 해소 표기 컨벤션과 동일). 새 unverified 항목이 생기면 출처 문서에 표시하고 여기 OQ-N으로 추가한다.

---

## 4. 교차 참조

| 대상 | 문서 | 절 |
|---|---|---|
| 타입 정의(정본) | [`15-data-contracts.md`](15-data-contracts.md) | §3, §4, §5, §7, §8 |
| 상태 머신·approval 생명주기·cleanup 불변식(규칙 권위) | [`04-normalized-agent-model.md`](04-normalized-agent-model.md) | §2, §4, §5 |
| Codex 매핑·unverified | [`ref-codex-app-server-protocol.md`](ref-codex-app-server-protocol.md) | §8, §10 |
| ACP 매핑·open questions | [`ref-acp-protocol.md`](ref-acp-protocol.md) | §13, §14 |
| Claude adapter launch/node/auth·unverified | [`ref-claude-agent-acp.md`](ref-claude-agent-acp.md) | §1, §3, §6 |
| transport·framing·WSL 경계·allowlist(절대경로+정확 args+env key, R4) | [`07-tauri-process-runtime.md`](07-tauri-process-runtime.md) | §8.1, 전체 |
| Codex adapter default 분기·미지원 server request 응답(R5) | [`05-codex-app-server-adapter.md`](05-codex-app-server-adapter.md) | §7.1 |
| Claude adapter default 분기·non-secret env(R1·R5) | [`06-claude-acp-adapter.md`](06-claude-acp-adapter.md) | §2.2, §3.3, §5 |
| 권한·보안·감사·redaction·untrusted renderer 위협모델(R4) | [`09-permissions-security.md`](09-permissions-security.md) | 전체 |
| allowlist 테스트·unknown-request 응답 테스트(R4·R5) | [`11-testing-acceptance.md`](11-testing-acceptance.md) | 전체 |
| persistence·transcript 정책·scrub·runtime scrollback replay(§1.12) | [`10-persistence-migration.md`](10-persistence-migration.md) | 전체, §4.7 |
| UI 구성·composer·thought/usage surface·transcript view-model(정본, §1.12) | [`08-ui-composition.md`](08-ui-composition.md) | 전체, §5 |
| seal 불변식·3-상태 residency·late-event 규칙(정본, §1.12) | [`04-normalized-agent-model.md`](04-normalized-agent-model.md) | §3.7 |
| UX 근거·open questions | [`research/ux-reference.md`](research/ux-reference.md) | §11, §12 |
| backend 코드 현실(allowlist·scrub·backpressure) | [`research/codebase-backend.md`](research/codebase-backend.md) | §2, §4, §6, §10 |
